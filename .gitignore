# Secrets — NEVER commit these
.env
.env.local
.env.*.local
google-service-account.json
*.pem
*.key
*.p8

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Node.js
node_modules/
npm-debug.log
yarn-error.log

# IDE / OS
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Claude Code internals (not project files)
.superpowers/

# Runtime artifacts — generated at dev time, never committed
uvicorn.log

# Graphify artifacts — large/binary; GRAPH_REPORT.md is committed, JSON is not
docs/_artifacts/graphify-output/ast-graph.json
docs/_artifacts/graphify-output/.graphify_python
docs/_artifacts/graphify-output/.graphify_root
docs/_artifacts/graphify-output/.graphify_detect.json
graphify-out/

# User-cloned MCP servers (not project code)
kindly-web-search-mcp-server/

# Voice agent call recordings (TESTING-ONLY; never commit audio files)
agent/recordings/*.mp3
agent/recordings/*.wav
agent/recordings/*.ogg
agent/vobiz_minimal/recordings/
