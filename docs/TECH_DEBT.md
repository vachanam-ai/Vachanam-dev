# Vachanam — Technical Debt Ledger

Every shortcut taken in this project is logged here with severity and a payback plan. Manager updates this on every sprint. If a row sits with no payback for too long, it gets escalated to the client.

A debt row stays until paid down. When paid down, move it to the "Paid down" section at the bottom with the date and the commit hash.

---

## Severity guide

| Level | What it means | Payback window |
|---|---|---|
| **P0 Critical** | Data leak risk, payment correctness risk, compliance risk | Same sprint, no exceptions |
| **P1 High** | Production-affecting if not addressed before scale | Within 2 sprints |
| **P2 Medium** | Affects developer velocity / maintenance cost | Within the current phase |
| **P3 Low** | Cosmetic / convenience | When touching the code anyway |

---

## Open debt

| ID | Severity | Date added | Owner specialist | Description | Why it was taken | Payback plan | Target sprint |
|---|---|---|---|---|---|---|---|
| ~~TD-001~~ | ~~P1~~ | ~~2026-05-22~~ | ~~database-engineer~~ | **CLOSED 2026-06-01 — see Paid down section.** Deleted broken 2fe8f201bc31 (dual-create enum bug + stale schema). Generated single clean migration ffcf1134aa8f covering all 10 tables. Applied. 29/29 tests still green. | | | |
| TD-018 | P2 | 2026-06-01 (scope reduced 2026-06-02) | database-engineer | Initial migration `ffcf1134aa8f` has ZERO non-unique indexes. Per database-engineer.md: every FK needs index (Postgres doesn't auto-index FKs). UNIQUE constraints already auto-index 5 columns (users.email, users.google_sub, branches.meta_phone_number_id, branches.whatsapp_number, organizations.owner_email). **SCOPE (revised 2026-06-02 per client decision):** Phase 4.5 ships **FK-only indexes** — `op.create_index` on every FK column across all 10 tables. Compound indexes — `(branch_id, date)` on tokens, `(branch_id, doctor_id, date)` for doctor schedule queries, `(phone)` on Patient, `(whatsapp_number)` on Doctor — are **DEFERRED to Phase 5** pending real `EXPLAIN ANALYZE` evidence from non-trivial query volume. We are not adding speculative compound indexes without measured query plans (write-cost is non-zero; evidence-based indexing only). | Autogen default doesn't emit indexes unless explicitly declared in schema.py via `index=True` or `Index(...)`. Brainstormer Pick 3 (Phase 4.5 plan validation, 2026-06-02) recommended evidence-based compound indexing; client accepted. | **Phase 4.5 (this sprint):** add second migration `phase45_fk_indexes` with `op.create_index` for every FK column. Update schema.py mapped_column with `index=True` for those columns. **Phase 5:** capture `EXPLAIN ANALYZE` from real query patterns (token lookup by branch+date, doctor schedule by branch+doctor+date, patient lookup by phone, doctor lookup by whatsapp_number). For each query where sequential scan > 100ms or > 10× index scan, add the targeted compound index in a separate migration with the evidence quoted in the migration docstring. | FK-only: Phase 4.5. Compound: Phase 7 or Phase 9 (evidence-gated; originally Phase 5, moved since WA deferred to MVP2 per client decision 2026-06-03). |
| TD-019 | P3 | 2026-06-01 | database-engineer | All FK constraints in initial migration default to NO ACTION ondelete. Per database-engineer.md rule 10: cascade behavior must be explicit (CASCADE or RESTRICT). Default NO ACTION ≈ RESTRICT but not portable. | Autogen doesn't infer ondelete; relationships in schema.py don't specify it | Update schema.py FK declarations to specify ondelete (CASCADE for child rows like Token deletes when Branch deleted = NEVER; mostly RESTRICT to force explicit data deletion path). Second migration to ALTER constraints. | Phase 4.5 (during data-lifecycle / DPDP review) |
| ~~TD-002~~ | ~~P2~~ | ~~2026-05-22~~ | ~~backend-engineer~~ | **CLOSED 2026-06-01 — see Paid down section.** Deleted `backend/payments_test_app.py`. Payments router now mounted in `backend/main.py` at `/api`. |  |  |  |
| ~~TD-003~~ | ~~P2~~ | ~~2026-05-22~~ | ~~backend-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Resolved as part of TD-004 — pricing now canonical Solo ₹1,999 + ₹3/min. | | | |
| ~~TD-004~~ | ~~P1~~ | ~~2026-05-22~~ | ~~manager~~ | **CLOSED 2026-05-29 — see Paid down section.** Client decided: canonical CLAUDE.md pricing (Solo/Clinic/Multi). Landing page UI mirror updated. | | | |
| TD-005 | P3 | 2026-05-22 | voice-agent-engineer | Emergency keyword `padipōyāḍu` (romanized Telugu) may not match Sarvam STT output (could need Telugu script `పడిపోయాడు`) | Easier to read in code; STT output not yet verified with real call | Verify on first real call in Phase 10; add Telugu script alongside if needed | Phase 10 |
| ~~TD-006~~ | ~~P2~~ | ~~2026-05-22~~ | ~~tester~~ | **CLOSED 2026-05-29 — see Paid down section.** Full pytest suite executed; 29/29 pass after TD-016 + TD-017 fixes. | | | |
| ~~TD-016~~ | ~~P1~~ | ~~2026-05-29~~ | ~~voice-agent-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Replaced module-level `redis_client` in booking_tools with `async with _redis()` per call. | | | |
| ~~TD-017~~ | ~~P1~~ | ~~2026-05-29~~ | ~~database-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Added `backend.database.engine.dispose()` before/after each test in conftest. | | | |
| ~~TD-007~~ | ~~P0~~ | ~~2026-05-29~~ | ~~voice-agent-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Replaced `_llm_with_fallback` with built-in `livekit.agents.llm.FallbackAdapter`. | | | |
| ~~TD-008~~ | ~~P0~~ | ~~2026-05-29~~ | ~~voice-agent-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Replaced `session.disconnect()` with `session.aclose()` (2 sites). | | | |
| ~~TD-009~~ | ~~P1~~ | ~~2026-05-29~~ | ~~voice-agent-engineer~~ | **CLOSED 2026-05-29 — see Paid down section.** Added `_solo_cap_watchdog` background polling task. | | | |
| ~~TD-010~~ | ~~P2~~ | ~~2026-05-29~~ | ~~tester~~ | **CLOSED 2026-05-29 — see Paid down section.** N=100 + boundary variant. | | | |
| ~~TD-011~~ | ~~P3~~ | ~~2026-05-29~~ | ~~tester~~ | **CLOSED 2026-05-29 — see Paid down section.** conftest uses `settings.redis_url`. | | | |
| ~~TD-012~~ | ~~P2~~ | ~~2026-05-29~~ | ~~tester~~ | **CLOSED 2026-05-29 — see Paid down section.** conftest pre-flushes Redis. | | | |
| ~~TD-013~~ | ~~P2~~ | ~~2026-05-29~~ | ~~manager~~ | **CLOSED 2026-05-29 — see Paid down section.** 8 obsolete docs moved to `docs/_legacy/`. | | | |
| TD-014 | P2 | 2026-05-29 | devops-engineer | `infra/Dockerfile.agent` and `infra/Dockerfile.backend` do NOT use a non-root user. `QUALITY_BAR.md` requires `USER app` after install. | Initial Dockerfiles were MVP; rule introduced after | Add `RUN groupadd -r app && useradd -r -g app ...` + `COPY --chown=app:app` + `USER app` to both Dockerfiles | Before Phase 10 |
| ~~TD-015~~ | ~~P1~~ | ~~2026-05-29~~ | ~~devops-engineer~~ | **CLOSED 2026-06-04 — see Paid down section.** `.github/workflows/ci.yml` added with two jobs: `test` (Python 3.11 + PG16 + Redis7 + pytest) and `secret-scan` (gitleaks OSS full-history scan). `.gitleaks.toml` added (allowlist for test fixtures). `.github/dependabot.yml` added (weekly pip + npm + actions updates). |  |  |  |
| TD-020 | P2 | 2026-06-01 | voice-agent-engineer | Pre-cached greeting WAV (Component 4 of voice flow spec) does not yet play directly via LiveKit Room track-publish API. Current implementation logs presence and falls back to live TTS. Target <100ms first-greeting latency not yet achieved — currently ~300-500ms (live TTS warmup time). | LiveKit Agents 1.5.9 does not expose a direct `publish_wav_to_room` API at the Agent layer. Need to use `ctx.room.local_participant.publish_track` with an audio source. Code path identified but not implemented in this iteration to keep diff small. | Implement WAV publish via livekit.rtc.AudioSource + publish_track in `_play_precached_greeting`. Requires WAV decoder for sample-rate conversion. ~1 day work. | Phase 10 acceptance |
| TD-021 | P2 | 2026-06-01 | voice-agent-engineer | STT confidence threshold check (Component 10 Layer A) not wired — livekit-plugins-sarvam 1.5.9 does not expose per-turn STT response object to the Agent layer. Only Layer B (LLM-side clarification detection) is active. | LiveKit abstracts STT under the AgentSession pipeline; raw Sarvam response not surfaced. | Either: (a) PR upstream to expose STT confidence in livekit.agents.stt events, OR (b) use sarvam SDK directly outside LiveKit pipeline and feed into agent. Option (a) preferred. | Phase 10 acceptance |
| TD-022 | P1 | 2026-06-02 | security-engineer | audit_log.metadata_json schema is JSONB (no shape enforcement at DB layer). Spec §8.3 + migration-log explicitly state "PII must NOT go into metadata_json — only IDs", but this is currently a convention only. Risk: a future @audit caller passes `{"patient_phone": "+919..."}` thinking it's helpful context; that PII lands in an append-only table with 7-year retention and no purge path. Audit log then becomes a long-tail PII liability under DPDP. | metadata_json is intentionally schema-less for flexibility; DB-level enforcement would defeat the purpose. | The @audit decorator (Phase 4.5 Task 7) MUST validate metadata_json against a PII denylist before write. Denylist: keys matching `^(phone|name|email|address|complaint|symptom)` (case-insensitive) → raise ValueError at decorator-write time + structlog warning. This is a code-layer enforcement of the doc-layer rule. Tests: tests/security/test_audit_log.py asserts metadata_json with `{"patient_phone": "..."}` raises before any DB write. | Phase 4.5 Task 7 (same sprint — @audit decorator must ship with this check) |
| TD-023 | P2 | 2026-06-02 | devops-engineer | audit_log table is currently writable + deletable by the vachanam_app role (default Postgres GRANT-all-to-owner). Spec §8.4 + migration-log explicitly state append-only enforcement requires GRANT INSERT, SELECT ON audit_log TO vachanam_app + REVOKE UPDATE, DELETE ON audit_log FROM vachanam_app, deferred to Phase 10 prod-init. Forward-compat verified by security-engineer review 2026-06-02 — no follow-up migration needed, just role-permission SQL in prod-init script. Risk: if Phase 10 ships without these GRANT/REVOKE lines, a compromised app token can rewrite audit history. | Cleanest separation of concerns — schema is created by migration as same-role admin, role-permission scoping is a prod-init concern, not a schema concern. Local dev uses single role (vachanam) for simplicity. | Phase 10 prod-init script (`scripts/prod_db_init.sql` or equivalent) must include: `GRANT INSERT, SELECT ON audit_log TO vachanam_app; REVOKE UPDATE, DELETE ON audit_log FROM vachanam_app;` AND `REVOKE TRUNCATE ON audit_log FROM vachanam_app;`. Plus tests: integration test in Phase 10 attempts UPDATE/DELETE via vachanam_app role, asserts permission-denied. | Phase 10 (prod cutover) |
| TD-024 | P1 | 2026-06-02 | backend-engineer | New CSP (Task 3) `script-src 'self' https://checkout.razorpay.com https://accounts.google.com` blocks inline scripts. Two existing static pages predate this CSP and use inline `<script>` blocks: (a) `backend/static/index.html` line 852 — Razorpay button-wiring IIFE (`startCheckout`, `showToast`, etc.); (b) `backend/static/razorpay-test.html` lines 72 (`onclick="pay()"` inline event handler) + 83 (`<script>` block with `pay()` definition). Real browsers will throw `Refused to execute inline script…` and silently fail BOTH the marketing landing page Razorpay flow AND the dev test page. curl smoke test caught the header values but cannot catch script-execution failure. Severity P1 because the landing page Razorpay flow is part of the paid-conversion path (Plan picker → Razorpay checkout). | The CSP is correct per spec §10.5 (locked-in defense against XSS via inline injection). The static HTML pages were authored before this CSP existed; this is a migration debt, not a spec drift. Three valid fixes; cheapest+strictest path = external `.js` files served same-origin. | **Preferred fix:** Extract `backend/static/index.html` lines 852-end (the IIFE) into `backend/static/checkout.js`. Extract `backend/static/razorpay-test.html` lines 83-end (the `pay()` definition) into `backend/static/razorpay-test.js`. Replace `onclick="pay()"` in razorpay-test.html line 72 with `id="payBtn"` (already present) + add `document.getElementById('payBtn').addEventListener('click', pay)` to the external js file. Both files load via `<script src="/static/checkout.js"></script>` — matches `'self'` in CSP. **Alternative:** add per-page nonce via Jinja template (more complex, needs request-scoped nonce). **Worst path:** add `'unsafe-inline'` to `script-src` (REJECTED — defeats the entire XSS defense). Verification after fix: open landing page in Chrome with DevTools → Console → assert ZERO `Refused to execute inline script` errors; click any plan button → assert Razorpay checkout opens. | Phase 4.5 Task 9 OR before Phase 7 (landing-page conversion testing) — whichever comes first. Hard-block on commercial launch (must be paid down before first real Razorpay conversion attempt). |
| TD-025 | P3 | 2026-06-03 | security-engineer | `backend/routers/queue.py` `get_today_queue` uses `except Exception` (line 105) instead of `except (SQLAlchemyError, OSError, RuntimeError)`. This catches genuine Python programming bugs (AttributeError, TypeError, NameError) and converts them to HTTP 500 + structlog.error, which is better than crashing but worse than letting them propagate as unhandled exceptions (which would get caught by main.py's catch-all handler with the same 500 result). The structlog.error call does log the error, so observability is not lost. Introduced to work around asyncio proactor `AttributeError` that escapes from asyncpg connection pool on Windows/Python 3.14 during concurrent test runs. | Windows/Python 3.14 asyncio proactor event loop incompatibility with asyncpg connection pool pings during concurrent test execution. Production (Linux/uvicorn) is unaffected. | Narrow the except clause to `except (SQLAlchemyError, OSError, RuntimeError) as exc:` OR fix at conftest layer by configuring asyncpg pool `pool_pre_ping=False` or using a SelectorEventLoop policy for tests. Prefer conftest fix since it addresses root cause. | Phase 7 (when touching queue.py for receptionist PWA) — originally Phase 5, moved since WA deferred to MVP2 per client decision 2026-06-03 |
| TD-026 | P2 | 2026-06-04 | backend-engineer | `backend/routers/auth.py` google_login handler: when a valid Google token resolves to an email NOT in the users table (line ~130, returns 403 "Not registered"), `user.login.failure` is NOT written to audit_log. Per spec §8.1 + §8.2, unauthorized-access probing with a real email should be auditable. An attacker who has valid Google tokens for emails not in the users table can probe the system indefinitely with no forensic trail. | Test gap: no RED test was written for this case in Task 6, so Task 7 implementer didn't cover it. Flagged by security-engineer Task 7 review. | TDD: tester writes RED test for "valid Google token + email not in users table → audit row with action='user.login.failure', metadata={'email': email} (allowed by login.failure exception)". Then backend-engineer adds the `await write_audit_row(...)` call in auth.py before the 403 raise. | Phase 4.5 (this sprint, after Task 9; or fold into Task 16 acceptance matrix) |
| TD-027 | P2 | 2026-06-04 | backend-engineer | No `data_retention.py` job exists. Privacy policy (docs/legal/privacy-policy.md section 7) commits to specific retention periods (bookings 2 years, audit 7 years, deleted accounts 30 days PII purge, Redis daily TTL). These retention periods are now published commitments -- "stated but not enforced" is a DPDP Act s.8 (storage limitation) compliance gap. Risk: PII accumulates indefinitely if no automated deletion runs; first DPB audit would flag this. | Retention enforcement was always Phase 6 scope per spec section 9.3 and gap analysis LC-9. Logged as TD now because the privacy policy is published, making the commitment binding. | backend-engineer creates `backend/jobs/data_retention.py` APScheduler daily job: (1) soft-delete patients with no activity > 2 years; (2) hard-delete patients soft-deleted > 30 days ago; (3) purge audit_log entries > 7 years old. Each deletion logged to audit_log. Spec in docs/legal/privacy-policy.md section 7. | Phase 6 (before first clinic goes live) |
| TD-028 | P2 | 2026-06-04 | backend-engineer | No `scripts/dsar.py` CLI tool exists. DSAR runbook (docs/runbooks/dsar.md) defines a manual SQL fallback for MVP1, but the 7-day SLA commitment in the published privacy policy requires a reliable, repeatable process. Manual psql queries are error-prone under pressure (wrong branch_id, forgotten table, missed audit entry). Risk: DSAR request handled incorrectly becomes a compliance incident. | Script was always Phase 6+ scope per gap analysis Action 7 and DSAR runbook section 4A. Logged as TD now because the DSAR runbook and privacy policy 7-day SLA are published, making the commitment binding. | backend-engineer creates `scripts/dsar.py` CLI: `--phone +91... --branch <uuid> --action export|correct|delete|withdraw`. Outputs JSON for export; executes SQL for correct/delete/withdraw; writes audit_log entry automatically. Tests: unit tests for each action type. | Phase 6 (before first clinic goes live) |
| TD-030 | P3 | 2026-06-06 | voice-agent-engineer | **CLAUDE.md RULE 7 documentation drift.** RULE 7 currently reads "speak branch.emergency_contact number, continue booking, never transfer". As of 2026-06-06 the implementation now does: SIP transfer via `ctx.transfer_sip_participant` → end session (fallback: speak number + end session). The CLAUDE.md master file still reflects the old rule. | Client override 2026-06-06: "direct SIP transfer = zero friction". Implementation changed in D-Emergency dispatch (commit pending). CLAUDE.md not updated in same commit to avoid touching non-agent files. | Update CLAUDE.md RULE 7 text to: "On emergency keyword detection, SIP-transfer the live call to branch.emergency_contact. Speak brief Telugu notice first. Release held token. End agent session. Fallback if SIP fails: speak number once, end session." | Next housekeeping pass (Phase 1 close or Phase 2 open) |
| TD-029 | **P1** | 2026-06-05 | security-engineer | **Shannon AI white-box scan deferred to Phase 3 exit gate.** Shannon is an AGPL-3.0 white-box AI pentester that reuses Vinay's Anthropic API key. Must run + 0 critical findings before Phase 9 onboarding work begins. Tests branch_id IDOR, JWT bypass, Razorpay HMAC verification, business logic flaws — security gaps that ZAP baseline (HTTP-only, no auth context) cannot cover. Setup steps documented in `project-shannon-zap-task17.md` memory file. AGPL-3.0 trigger does not apply (private use only, no distribution). | Shannon scan was Phase 4.5 Task 17b. Deferred because: (1) running it at end of Phase 3 produces a single comprehensive scan covering all backend + frontend code, vs running partial scans per phase; (2) Anthropic key not yet in .env; (3) net saving of ~2 opus dispatches by consolidating. | Vinay adds `ANTHROPIC_API_KEY` to `.env`. security-engineer runs Shannon against full backend + frontend codebase. 0 critical findings = gate passes. Any critical finding = fix before Phase 9 onboarding dispatch. | Phase 3 exit gate (P1 release blocker for Phase 9) |
| TD-031 | P3 | 2026-06-06 | voice-agent-engineer | **Migrate voice stack from LiveKit to Pipecat (deferred, conditional).** Spike 2026-06-06 confirmed Pipecat = better conceptual fit (built for 1-on-1 voice agents vs LiveKit's multi-participant focus) but rewrite = 4-6 days, same cost, must write own concurrency supervisor. Path A chosen (strip current LiveKit stack). | LiveKit stack already in production path; rewrite cost-neutral but burns 4-6 days with no feature gain. Conditional on LiveKit-specific pain emerging post-launch. | Trigger condition: if LiveKit-specific pain emerges in production (SIP bridge issues, concurrency dispatch limits, plugin lock-in). Payback: ~4-6 days to rewrite agent.py + tools on Pipecat pipeline API. Preserve booking_tools business logic, Redis token assignment, branch isolation, audit_log. Swap transport from LiveKit SIP to WebSocket via Vobiz X-Pipecat repo or via Daily.co intermediary. | Post-launch (conditional — only if trigger fires) |
| TD-032 | P2 | 2026-06-06 | database-engineer | **Alembic conftest teardown leaves orphaned `alembic_version` stamp.** `tests/conftest.py:29` calls `Base.metadata.drop_all` at teardown, wiping all tables but leaving `alembic_version` row stamped at `8559268c0c44`. Recovery procedure after any full test run: `alembic stamp base`, then `alembic stamp ffcf1134aa8f`, then `alembic upgrade head`. Migration 2 is a full schema rebuild from Phase 4.5 autogen, can't apply on top of bare `alembic_version`. | conftest teardown was designed for test isolation (drop/create all tables per test) without considering Alembic version table side-effects. Pre-existing since Phase 4. | Split migration `8559268c0c44` into incremental additions only (remove tables that exist from migration 1). OR: change conftest to drop+recreate `alembic_version` row to base instead of dropping tables. Easier fix = conftest change: after `drop_all`, run `alembic_version.delete()` then `INSERT alembic_version SET version_num='base'`. | Phase 2 (next backend touch) |
| TD-033 | P3 | 2026-06-06 | manager | **CLAUDE.md RULE 7 amendment needed.** 2026-06-06 client decision overrode CLAUDE.md RULE 7 — emergency handling now does SIP REFER to `branch.emergency_contact` instead of speaking the number. Implemented in D-Emergency (commit `d0eb08e`). CLAUDE.md text still says "give branch.emergency_contact, continue booking, never transfer". Overlaps with TD-030 (same drift, different owner). | TD-030 tracks the voice-agent-engineer implementation side. TD-033 tracks the CLAUDE.md master-doc amendment itself, which is a manager-owned edit. Separated because the doc edit requires client sign-off on new rule text. | 5-min edit of CLAUDE.md RULE 7 paragraph. Replace "give branch.emergency_contact, continue booking as normal, never transfer" with new transfer-first/fallback-spoken behavior. Cite `feedback-emergency-transfer` memory. Requires client sign-off on exact wording. | Phase 1 close or Phase 2 open (next housekeeping pass) |
| TD-034 | P2 | 2026-06-06 | devops-engineer | **Local DID per clinic at Phase 4 onboarding.** Testing uses out-of-region (Karnataka) no-commit Vobiz DID. At clinic onboarding, patients calling a non-local area code may perceive as spam, hurting answer rates. Per `project-vobiz-region-test` memory. | Vobiz test DID was the only available no-commit option for development; production clinics need local-region DIDs for patient trust. | At Phase 4 onboarding flow build, add step to provision local Vobiz DID matching clinic's city (Hyderabad/Mumbai/Bangalore etc.) with 6-month commitment accepted. Bake commit cost into Solo/Clinic/Multi plan pricing (already covers Rs 1,000/mo DID line per CLAUDE.md cost table). | Phase 9 (clinic onboarding build) |
| TD-035 | P2 | 2026-06-06 | devops-engineer | **`provision_vobiz_trunk.py` blind to credential rotation.** When Vobiz trunk creds rotated (new SIP domain + username + password from a new no-commit DID), provision script's name-based idempotency skipped Steps 1-3 ("already exists"), leaving LiveKit trunks with stale Vobiz creds. Manual teardown required (delete old LiveKit trunks via livekit-api SDK, then re-run). Captured in DISPATCHES.md 2026-06-06. | Provision script designed for single-use setup, not credential rotation. Idempotency check uses trunk name only, not address or auth fields. | Enhance provision_vobiz_trunk.py Steps 1-2 to compare existing trunk's `address` + `auth_username` against current .env values. If mismatch, delete + recreate. Or: add explicit `--force-recreate` CLI flag. ~30 LOC. Add unit tests. | Phase 9 (clinic onboarding build) |
| TD-036 | **P1** | 2026-06-06 | devops-engineer | **Vobiz DID onboarding pre-flight checker.** Diagnosed Vobiz inbound failure ("you are not allowed to dial this number") = account `is_verified=false` + DID `provider=""` + recycled number (released April, repurchased June). All 3 are Vobiz-upstream gates invisible from our wiring side. Cost hours of debugging before root cause clear. Per `project-vobiz-kyc-blocker` memory. | No pre-flight diagnostic existed; failure mode was opaque (Vobiz returns generic rejection, no indication of KYC/provider/recycled-number root cause). | Write `scripts/check_vobiz_did_ready.py` that asserts: (1) `GET /Account/{auth_id}` returns `is_verified=true`, (2) `GET /Account/{auth_id}/numbers` returns DID with `provider != ""` and `usage_status != ""`, (3) DID `released_at` either null or >72h old. Fail loudly with exact diagnostic per check. Run as part of clinic onboarding wizard at Phase 9. ~50 LOC + tests. | Phase 9 (release blocker for clinic onboarding) |

---

## Paid down

| ID | Severity | Date paid | Commit | Resolution |
|---|---|---|---|---|
| TD-007 | P0 | 2026-05-29 | *(pending)* | Replaced raw `google.LLM` with `livekit.agents.llm.FallbackAdapter([Gemini, GPT-4o-mini])` in `agent/agent.py`. Built-in adapter handles failover transparently per call. Removed unused `_llm_with_fallback` function. |
| TD-008 | P0 | 2026-05-29 | *(pending)* | Replaced `session.disconnect()` with `session.aclose()` in `agent/agent.py` (2 call sites). LiveKit Agents 1.4 uses `aclose()` for session shutdown. |
| TD-009 | P1 | 2026-05-29 | *(pending)* | Added `_solo_cap_watchdog` background asyncio task in `agent/agent.py`. Polls every 5s, fires warning at SOLO_CAP_SECONDS-10 (gated by `solo_warning_sent`), closes session at SOLO_CAP_SECONDS. Cancelled in entrypoint's `finally` block on session end. Removed duplicate logic from `on_user_turn_completed`. |
| TD-010 | P2 | 2026-05-29 | *(pending)* | Rewrote `tests/edge_cases/test_concurrent_tokens.py`. Now: (1) `test_100_concurrent_callers_get_unique_sequential_tokens` runs N=100 with `daily_token_limit=200`; (2) `test_10_concurrent_callers_at_limit_boundary` pre-fills 99, races 10 for the last, asserts exactly 1 success + 9 `full` + Redis counter exactly 100 (rollbacks verified). |
| TD-011 | P3 | 2026-05-29 | *(pending)* | Replaced hardcoded `"redis://localhost:6379"` with `settings.redis_url` in `tests/conftest.py`. |
| TD-012 | P2 | 2026-05-29 | *(pending)* | Added `await r.flushdb()` BEFORE the `yield` in conftest's redis fixture. Prevents previous-test pollution. |
| TD-013 | P2 | 2026-05-29 | *(pending)* | Moved 8 obsolete docs to `docs/_legacy/`: PHASE_0..5_*.md (root), `docs/vachanam-progress.md`, `docs/superpowers/plans/2026-05-18-phase-2-backend.md`. Added `docs/_legacy/README.md` explaining archaeology-only purpose. |
| TD-004 | P1 | 2026-05-29 | *(pending)* | Client decision: keep canonical pricing from CLAUDE.md (Solo ₹1,999 + ₹3/min, Clinic ₹7,999 flat / 2,100 min, Multi ₹16,999 flat / 4,200 min / 2 branches). Reject vachanam.in live Starter/Growth/Unlimited tier names + amounts. |
| TD-003 | P2 | 2026-05-29 | *(pending)* | Resolved by TD-004 closure. Landing page mirror pricing section rewritten with canonical Solo/Clinic/Multi cards (₹1,999 / ₹7,999 / ₹16,999). data-amount attributes updated to 199900 / 799900 / 1699900 paise. Core UI (color #006B6B teal, Outfit/Spectral/Pacifico fonts, layout structure) unchanged per client instruction. |
| TD-006 | P2 | 2026-05-29 | *(pending)* | Full pytest suite executed against Docker Postgres 16 + Redis 7. 29/29 tests pass after TD-016 + TD-017 fixes (3 prior failures were event-loop binding bugs in production code, exposed by test runner — not test code defects). Baseline established for Phase 4. |
| TD-016 | P1 | 2026-05-29 | *(pending)* | Discovered during Phase 4 prep test run. Module-level `redis_client = aioredis.from_url(...)` in `agent/tools/booking_tools.py` bound to first event loop at import. Failed with `RuntimeError: Event loop is closed` on subsequent test loops AND would fail on uvicorn worker restart in production. Fixed: replaced with `_redis()` factory + `async with _redis() as r:` per call. Cost: ~1-2ms per Redis op on localhost. Production-safe under any loop topology. |
| TD-017 | P1 | 2026-05-29 | *(pending)* | Discovered during Phase 4 prep test run. Module-level `engine` in `backend/database.py` pooled connections across pytest-asyncio test loops (mode=auto). Pool reuse triggered `_check_closed` on stale loops. Fixed: conftest's `db` fixture now calls `backend.database.engine.dispose()` before AND after each test, forcing a fresh pool per loop. Test-only change — production keeps the pooled engine. |
| TD-001 | P1 | 2026-06-01 | *(pending)* | Deleted broken `alembic/versions/2fe8f201bc31_initial_schema.py` (dual-create ENUM bug — explicit `enum.create()` + implicit `Enum()` column creation → "type already exists" failure). Generated single clean migration `ffcf1134aa8f_initial_schema_with_user_table.py` via autogen against current schema.py. Applied to fresh DB. All 10 tables present (users, branches, doctors, patients, tokens, calls, followup_tasks, billing_cycles, whatsapp_sessions, organizations) + alembic_version. 29/29 tests still pass after migration applied. Deleted-and-regenerated is acceptable here because the old migration never successfully ran in production. |
| TD-002 | P2 | 2026-06-01 | *(pending)* | Deleted `backend/payments_test_app.py`. Standalone scaffolding no longer needed — `backend/main.py` now mounts the payments router at `/api`, serves the landing page at `/`, and serves the dev test page at `/dev/test` (404 in production). Smoke-tested: `POST /api/create-order` returns real Razorpay order. |

| TD-015 | P1 | 2026-06-04 | `76cd7c3` | Created `.github/workflows/ci.yml` (test job: Python 3.11 + PG16 + Redis7 + alembic + pytest; secret-scan job: gitleaks OSS full-history). Created `.gitleaks.toml` (default OSS ruleset + allowlist for test fixture phones, ci.yml test secrets, .env.example). Created `.github/dependabot.yml` (weekly pip backend + agent, npm frontend future-proof, github-actions). |

When closing a future row, append here with this format:
```
| TD-XXX | severity | date paid | commit hash | how it was resolved |
```

---

## Rules

- Every shortcut creates a row here — no silent shortcuts
- Every row has an owner specialist and target sprint
- Manager reviews open debt every sprint planning
- P0 debt that misses its sprint = escalation to client
- P1 debt overdue twice = escalation
- Paid-down rows are NEVER deleted — they're moved to the bottom for historical record
- When a row blocks a feature, link the feature task to this row

| TD-019 | P1 | 2026-06-12 | *(pending)* | Razorpay verify-payment validates the HMAC but persists NOTHING (no BillingCycle row, no org status change) and razorpay_webhook_secret has no webhook route (bug-bounty B10). A 'verified' payment does not activate/extend any subscription. Must be wired in the Razorpay billing phase BEFORE first paying clinic. |
