# FIXLOG — every fix, its proof, and its regression status

Process (Vinay, 2026-06-12): every fix lands here with a regression guard.
After EVERY new fix: (1) run the full suite — all guards below must stay
green; (2) re-check any ⚠ manual items touched by the change. Step-by-step
upgrades, nothing previously fixed may silently break.

Legend: ✅ automated regression test · ⚠ manual check (no automated guard yet)

| # | Date | Issue (symptom) | Root cause | Fix commit | Regression guard |
|---|------|-----------------|------------|-----------|------------------|
| 1 | 06-11 | Inbound ringing, never answered | Hard `wait_for_participant()` before `session.start()` + missing asyncio import | d569103 | ⚠ manual call test |
| 2 | 06-11 | Telugu horrible + tools failing all call | `attempt_timeout=3s` < Google min 10s deadline → every turn fell to GPT-4o-mini, which hallucinated doctor UUIDs | 8dceff7 | ⚠ config — comment in `_build_fallback_llm` |
| 3 | 06-11 | Booking failed after 2 retries | Wrong calendar class injected (new kwargs vs legacy signature) → TypeError | 6a86912 | ✅ test_bug_fixes (calendar path exercised) |
| 4 | 06-11 | Routing always default doctor | Gemini wraps JSON in ``` fences → parse fail | 6a86912 | ⚠ fence-strip in route_to_doctor |
| 5 | 06-11 | 5-8s replies | Gemini 2.5 Flash thinking ON by default | 6a86912 | ⚠ thinking_budget=0 both LLM paths |
| 6 | 06-11 | Garbage reg data accepted | No validation | 069a1b2 | ✅ test_validators + test_register_otp |
| 7 | 06-11 | Clinic B could claim Clinic A's DID | No uniqueness on did_number | e008e62 | ✅ test_did_hijack |
| 8 | 06-11 | Agent thought today = June 19, refused valid dates | LLM has no clock; guessed wrong year | 18518de | ⚠ TODAY line injected per call |
| 9 | 06-11 | Family booking attached to wrong patient | Patient matched by phone only | 18518de | ✅ test_register_otp + dup-guard tests |
| 10 | 06-11 | Call #2 onward rang forever | Module-level SQLAlchemy engine bound to call #1's dead event loop | 0c0b30c | ✅ whole suite runs on loop-aware engine |
| 11 | 06-11 | "already booked" echo after success | Second confirm_booking hit dup guard | 0c0b30c | ⚠ prompt step 7 |
| 12 | 06-11 | New doctors missing from calendar | Hours sync was token-doctors-only | 0c0b30c | ⚠ doctors router `_maybe_upsert` |
| 13 | 06-12 | Spoken phone garbled → split patients | No phone validation on voice path | bbcfb6b | ✅ invalid_phone path in confirm_booking |
| 14 | 06-12 | Confirmed slot looked free (dbl-book risk) | Redis restart wiped slot keys; Redis was sole truth | bbcfb6b | ✅ DB-count check in assign/availability |
| 15 | 06-12 | Free 4pm slot reported "not free" | LLM passed zero-width window (4pm-4pm) | bbcfb6b | ⚠ window expansion in check_availability |
| 16 | 06-12 | Doctor on leave: nobody called patients | No job dispatched cascade_rebook tasks | bbcfb6b | ⚠ scheduler job registered (main.py) |
| 17 | 06-12 | Hung up on patient's question (rebook) | LLM judged question as decline | 08eb6ea | ⚠ end_call STRICT docstring + prompt |
| 18 | 06-12 | ISO date read digit-by-digit | Raw date into TTS template | 65f2679 | ✅ telugu_dates assertions |
| 19 | 06-12 | "Rescheduled" but nothing in calendar, old cancelled | LLM treated assign_token hold as booked, skipped confirm | 0d79fe1 | ✅ cancel hard-guard + test_bug_fixes |
| 20 | 06-12 | Rebook call restarted new-patient flow on mumble | Generic booking flow overrode outbound context | 6e9ba5b | ⚠ identity-known override in both EXTRAs |
| 21 | 06-12 | /availability reload → {"detail":"Not Found"} | Vite proxied page-route prefixes to FastAPI | e3cea9c | ⚠ html-bypass in vite.config |
| 22 | 06-12 | One booking became two on June 14 | Reschedule orchestration split across LLM steps | 4b5e7bb | ✅ test_reschedule_atomic_one_confirmed_booking |
| 23 | 06-12 | (latent) transient calendar error → duplicate rows | Function-level @retry re-ran with pending Token in session | 4b5e7bb | ✅ test_confirm_booking_transient_calendar_failure_single_row |
| 24 | 06-12 | (latent) cancelled token number reissued to next patient | Cancel DECR'd the token counter (= number sequence) | 4b5e7bb | ✅ test_cancelled_token_number_never_reissued |
| 25 | 06-12 | Same-day reschedule blocked as "already booked" | Dup guard saw the booking being replaced | 4b5e7bb | ✅ test_reschedule_atomic (would fail without exclude) |
| 26 | 06-12 | Rebook call: "you don't have any booking" | find_my_bookings only returns confirmed; cascade had cancelled it | 06-12 charts commit | ✅ test_find_bookings_includes_recent_cancelled |
| 27 | 06-12 | Rebook call: "unable to find details of Dr Test Kumar" | LLM passed unmatched doctor name; state had no doctor pre-selected on outbound | 06-12 charts commit | ⚠ doctor_id now in dispatch metadata → state pre-select (both jobs) |
| 28 | 06-12 | Leave invisible on owner dashboard | Card existed but per-date rows; now grouped "from X to Y" ranges; API verified returning rows (manual JWT test) | 06-12 charts commit | ⚠ hard-refresh / re-login needed for stale sessions |
| 29 | 06-12 | Booking confirmed without the year | No year rule in prompt; "year only when it matters" left it out of read-backs | 06-12 test-round-3 | ⚠ prompt DATES rule + step 6 (year mandatory in confirmations) |
| 30 | 06-12 | Asked time full → no nearby suggestion | Availability listed ALL free windows, not the ones nearest the asked time | 06-12 test-round-3 | ✅ test_check_availability_offers_nearest_time |
| 31 | 06-12 | Arm pain routed to dentist | Router LLM forced to match; unmatched fell to DEFAULT doctor — no out-of-scope path existed | 06-12 test-round-3 | ✅ test_route_out_of_scope_lists_specialties + vague-still-defaults |
| 32 | 06-12 | Random ధన్యవాదాలు, call ended | STALE prompt block claimed "system ends call after 3 repeats" (no such handler in LiveKit agent — LLM emulated it); no code guard on end_call | 06-12 test-round-3 | ✅ test_end_call_blocked_while_booking_unconfirmed + prompt block replaced |
| 33 | 06-12 | Patient details sometimes not asked | Prompt-only rule, nothing enforced | 06-12 test-round-3 | ✅ test_confirm_booking_first_time_patient_requires_age (+ known-patient skip) |
| 34 | 06-12 | Rebook call: asked cancel → "no booking existed" | Booking already cancelled_by_clinic; cancel returned bare not_cancellable_*; no decline tool — retry loop kept calling every 30min | 06-12 test-round-3 | ✅ test_decline_rebook_completes_followup_task + already_cancelled instruction |
| 35 | 06-12 | Correct name+number → "not matching" | find_my_bookings exact-matched phone strings; SIP caller-ID formats (+91/0/bare) ≠ stored E.164 → empty result → agent asked number → STT garbled it | 06-12 test-round-3 | ✅ test_find_bookings_matches_any_phone_format (4 formats) |
| 36 | 06-12 | "Book at 3" booked 3 AM (outside doctor hours) | confirm_booking trusted the LLM's appointment_time blindly (no working-hours check, token-doctor strays stored); assign_token skipped ALL validation when slot grid empty; no hard hours bound anywhere; no prompt rule for bare spoken numbers | 06-12 3am-fix | ✅ 4 tests: assign+confirm refuse outside hours, token stray time dropped, unconfigured schedule refused; prompt TIME INTERPRETATION rule |
| 37 | 06-12 | (bounty B1) Cancelled appointments stayed on Google Calendar forever | cascade_cancel enqueued delete tasks with calendar_id=None; writer used it verbatim → 5 fails → failed_permanent | 06-12 bounty-r1 | ✅ test_cascade_delete_resolves_calendar_id |
| 38 | 06-12 | (bounty B2) Outbound calls would hit wrong tenant with 2+ clinics | No branch_id in dispatch metadata; agent fell back to global DID for outbound | 06-12 bounty-r1 | ⚠ branch_id in both jobs' metadata + agent prefers it for outbound |
| 39 | 06-12 | (bounty B3) Queue/analytics/walk-in/stop-walkins "today" = server UTC | date.today() on UTC server ≠ branch-local date 00:00–05:30 IST → bookings invisible in that window | 06-12 bounty-r1 | ⚠ _branch_today helper used in queue, walk-in, analytics, stop-walkins |
| 40 | 06-12 | (bounty B4) Queue showed booking-creation timestamp as appointment time | PatientEntry lacked appointment_time; UI sliced confirmed_at (UTC) | 06-12 bounty-r1 | ⚠ appointment_time in payload + Queue.jsx renders it |
| 41 | 06-12 | (bounty B5) Late-night reminders silently never fired | time-only window wrapped past midnight (lo>hi matched nothing) | 06-12 bounty-r1 | ✅ test_reminder_window_midnight_wrap + afternoon case |
| 42 | 06-12 | (bounty B6) Ambiguous doctor name silently booked first match | _resolve_doctor_id substring matched, returned first of several | 06-12 bounty-r1 | ✅ test_resolve_doctor_id_ambiguous_name_refuses |
| 43 | 06-12 | (bounty B7, latent) LLM-echoed token_number persisted | confirm_booking stored the model's copy, discarding the Redis-reserved value | 06-12 bounty-r1 | ⚠ agent wrapper overrides with state.token_number when held |
| 44 | 06-12 | (bounty B8) Phoneless reminder tokens rescanned every minute forever | no-phone branch set reminder_sent but never committed | 06-12 bounty-r1 | ⚠ commit moved before phone check |
| 45 | 06-12 | (bounty B9+B11) 50 calendar clients/tick; analytics raw-string UUID | per-task GoogleCalendarService(); no 400 guard on branch_id | 06-12 bounty-r1 | ⚠ one svc per tick; UUID validated before access check |
| 46 | 06-12 | 20 fake "Replay Clinic" orgs + false ₹7,999 revenue on admin console | test_otp_code_is_single_use lacked the db fixture → its successful /register wrote to the PRODUCTION Neon DB on every suite run; my Console-Clinic fixture rode the same hole earlier | 06-12 fresh-start | ✅ db param added with explanatory docstring; AST sweep confirmed it was the only app-WRITING test without db; DB wiped via scripts/wipe_clinics.py (orgs 20→0, users 24→3 super_admins kept, Redis flushed) |
| 47 | 06-12 | Admin chart: identical fake expense bars in empty months; hover showed nothing | history months were charged TODAY's DID rent; SVG had no tooltips | 06-12 fresh-start | ⚠ DID rent current-month only; native SVG title tooltips on bars + full-column hover targets + usage-bar title |
| 48 | 06-13 | (bounty C1) confirm_booking with no held token = unguarded double-booking | LLM skips assign_token → zero capacity check; only phone dup guard | 06-13 bounty-r2 | ✅ test_confirm_without_assign_respects_slot_capacity + _token_limit (re-check inside confirm tx) |
| 49 | 06-13 | (bounty H1) Redis flush re-issues token 1 over confirmed DB tokens | token path trusted Redis alone (slot path was already fixed #14) | 06-13 bounty-r2 | ✅ test_token_counter_floored_after_redis_flush |
| 50 | 06-13 | (bounty H2) cancel/reschedule deletes from wrong calendar → ghost events | _do_cancel used branch cal; create used doctor cal | 06-13 bounty-r2 | ⚠ _do_cancel resolves doctor cal then branch fallback (mirror create) |
| 51 | 06-13 | (bounty H3) failed reschedule leaks Redis slot hold until TTL | _do_reschedule used module assign_token; state never set, cleanup blind | 06-13 bounty-r2 | ⚠ _release_hold DECRs on confirm failure (slot keys only) |
| 52 | 06-13 | (bounty H4) solo 4-min call cap unenforced (TD-009 regression) | watchdog lost in LiveKit port; state.plan always "clinic" | 06-13 bounty-r2 | ⚠ org plan → state.plan; asyncio watchdog warns T-10 then closes at cap |
| 53 | 06-13 | (bounty H5) trials never expire → free AI service forever | no trial_pause job; call_blocked ignored trial_ends_at | 06-13 bounty-r2 | ✅ test_call_blocked_expired_trial + daily trial_pause job |
| 54 | 06-13 | (bounty M1) N workers = N schedulers → duplicate calls/writes | every lifespan ran its own APScheduler, plain-SELECT claims | 06-13 bounty-r2 | ⚠ Postgres advisory-lock leader election; only leader schedules |
| 55 | 06-13 | (bounty M2) tasks stranded in_progress after a crash, never retried | status set in_progress then crash; poll only sees pending | 06-13 bounty-r2 | ⚠ requeue_stale_in_progress sweep every 5 min |
| 56 | 06-13 | (bounty M3-M6/L8) walk-in: raw phone, name-blind match, no dup guard, hold leak, past slot, UUID 500 | desk path never got the voice path's guards | 06-13 bounty-r2 | ⚠ normalize_indian_phone + (phone,name) match + dup 409 + guarded DECR + M6 past-slot + L8 400 |
| 57 | 06-13 | (bounty M7) email-OTP failure burns phone OTP → signup dead-end | register only checked verify_code, never is_verified | 06-13 bounty-r2 | ⚠ register accepts verify_code OR is_verified per channel |
| 58 | 06-13 | (bounty M8) prod could echo OTP codes in API response | otp_dev_echo default True, absent from .env.example | 06-13 bounty-r2 | ⚠ otp_echo_enabled forces off when APP_ENV=production; OTP vars added to .env.example |
| 59 | 06-13 | (bounty M9) concurrent same-email register → 500 not 409 | check-then-insert, no IntegrityError handling | 06-13 bounty-r2 | ⚠ register catches IntegrityError → 409 |
| 60 | 06-13 | (bounty M10/L3) leave cascade allowed past dates + unbounded range | only date_from<=date_to validated | 06-13 bounty-r2 | ⚠ reject date_to<branch_today; cap range 365d |
| 61 | 06-13 | (bounty M11) DID format mismatch aborts every inbound call | DID stored verbatim; agent exact-matched | 06-13 bounty-r2 | ✅ test_normalize_did_forms (normalize on write + agent lookup) |
| 62 | 06-13 | (bounty M12) billing month boundary in UTC not IST | hard-block gate + admin overview used UTC month-start | 06-13 bounty-r2 | ⚠ IST month boundary in agent gate + admin overview; IST bucketing |
| 63 | 06-13 | (bounty M13) analytics call-day buckets UTC vs branch-local tokens | func.date on timestamptz truncated in UTC | 06-13 bounty-r2 | ⚠ func.timezone(branch_tz, started_at) for call day/month buckets |
| 64 | 06-13 | (bounty M14) cascade frees DB slots but not Redis keys | cascade never touched Redis; un-leave left slots "full" till TTL | 06-13 bounty-r2 | ⚠ cascade DECRs slot keys for cancelled slot tokens |
| 65 | 06-13 | (bounty M15) backend jobs silently no-op without LiveKit creds; env drift | jobs read os.getenv directly; vars absent from .env.example | 06-13 bounty-r2 | ⚠ config livekit fields + voice_plane_configured + WARNING log; .env.example updated |
| 66 | 06-13 | (bounty L1/L2/L4/L6) rebook stuck in_progress; lost cal-delete silent; doctor card match broken; reminder time read digit-by-digit | various | 06-13 bounty-r2 | ✅ test_telugu_time_spoken; ⚠ unreachable status, enqueue-lost alert, DoctorOut.user_id |
| 67 | 06-13 | (bounty T1) C1's capacity re-check was TOCTOU — concurrent confirms still double-book | re-count+insert not serialized; promised unique index never added | 06-13 bounty-r3 | ✅ test_duplicate_token_number_rejected_by_unique_index (partial unique index uq_token_number_confirmed on model+migration; agent wrapper acquires atomic Redis hold before confirm when none held — respects max_concurrent; token-number pre-check; wrapper rollback on failure) |
| 68 | 06-13 | (bounty T2) solo 4-min cap shipped DISABLED (default 0 = unlimited) | watchdog only ran when cap>15; env default 0 | 06-13 bounty-r3 | ⚠ solo cap defaults to 240s even when env is 0 |
| 69 | 06-13 | (bounty T3) doctor calendar_id change orphans old recurring event, fails on new | upsert PATCHed old event id against new calendar → 404 | 06-13 bounty-r3 | (deferred TD-023 — needs doctors-router recurring-sync rework) |
| 70 | 06-13 | (bounty T4) reschedule hold leaked on a hard call-drop between assign and confirm | _do_reschedule never set state hold; cleanup couldn't release | 06-13 bounty-r3 | ⚠ state hold set right after reschedule assign; _clear_hold avoids double-DECR |
| 71 | 06-13 | (bounty T5) scheduler advisory lock not released on graceful shutdown | leader_conn.close() returns fairy to pool, lock persists | 06-13 bounty-r3 | ⚠ explicit pg_advisory_unlock before close on shutdown |
| 72 | 06-13 | (bounty T6) admin blocked_now wrong for expired trials | call_blocked called without trial_ends_at | 06-13 bounty-r3 | ⚠ admin overview passes trial_ends_at (matches agent gate) |
| 73 | 06-13 | (bounty T8) orphan compiled migration .pyc | leftover from deleted initial migration | 06-13 bounty-r3 | ⚠ .pyc deleted (gitignore already covers __pycache__) |
| 74 | 06-13 | (bounty F2/F8) token-doctor bookings hard-FAIL without a Google Calendar + write an unwanted per-patient event when one exists | confirm_booking called create_booking_event for ALL types; CalendarNotConfiguredError aborted the most common plan (token queue) by voice; reschedule inherited it | 06-13 bounty-r4 | ✅ test_token_booking_succeeds_without_calendar; test_confirm_booking_transient_calendar_failure_single_row retargeted to slot doctor (calendar write is slot-only per spec §6.5) |
| 75 | 06-13 | (bounty F4) missing SIP DID attr could route a caller to the WRONG tenant (DPDP leak) | did fell back to env VOBIZ_DID and resolved whatever branch matched it, regardless of who was dialed | 06-13 bounty-r4 | ⚠ fallback DID refused when >1 branch exists (multi-tenant); call aborts rather than risk cross-tenant context |
| 76 | 06-13 | (bounty F3) OTP code echoed in API response on a provider failure in any non-prod env | echo gated only on otp_echo_enabled; a configured-but-failing provider still leaked the real code → self-verify any phone/email | 06-13 bounty-r4 | ✅ test_otp_not_echoed_when_provider_configured_but_send_fails — echo only when NO provider configured (fail closed) |
| 77 | 06-13 | (bounty F16) clinic/multi (or mis-resolved solo) calls could run UNBOUNDED, burning per-minute money | solo cap watchdog only fired for plan=="solo"; a plan-resolution failure defaulted to "clinic" and dodged the cap | 06-13 bounty-r4 | ⚠ absolute 900s safety ceiling on every call regardless of plan (solo still 240s) |
| 78 | 06-13 | (bounty F14) analytics plan lookup compared UUID column to raw str path param | copy/paste used branch_id not the validated branch_uuid | 06-13 bounty-r4 | ⚠ use branch_uuid (consistent with the rest of the router) |
| 79 | 06-13 | (bounty F7) JWT lived 24h vs the documented 8h contract — larger stolen-token blast radius | config default + .env drift (24) overrode the 8h auth contract | 06-13 bounty-r4 | ✅ test_jwt_expire_default_is_8h; default + .env + .env.example set to 8 |
| 80 | 06-13 | (bounty G5) doctor-role staff login was never linked to a Doctor row → /my-schedule blank, orphan account | add_staff never set Doctor.user_id | 06-13 bounty-r5 | ✅ test_doctor_role_login_links_to_doctor + _without_match_rejected (link by doctor_id or invited_email; refuse if none) |
| 81 | 06-13 | (bounty G6) staff/doctor logins accepted weak/all-numeric passwords (bare len>=8) | add_staff didn't run validate_password | 06-13 bounty-r5 | ✅ test_staff_weak_password_rejected (validate_password on staff create) |
| 82 | 06-13 | (bounty G8) multiple/zero default doctors → non-deterministic out-of-scope routing | is_default_doctor had no uniqueness; create/update never demoted others | 06-13 bounty-r5 | ✅ test_single_default_doctor_per_branch (promote demotes the rest, create + update) |
| 83 | 06-13 | (bounty G9) changing a branch DID left the OLD number on the LiveKit trunk (latent cross-tenant routing if reassigned) | update_branch_settings only added the new DID | 06-13 bounty-r5 | ⚠ remove_did_from_inbound_trunk called for the old DID on change |
| 84 | 06-13 | (bounty G12) create-order reflected the raw Razorpay error string to the client | detail=f"Razorpay rejected: {e}" | 06-13 bounty-r5 | ⚠ generic message, full error logged |
| 85 | 06-13 | (bounty G13) Token.confirmed_at written with naive datetime.utcnow() into a tz-aware column (5.5h drift risk) | deprecated naive utcnow | 06-13 bounty-r5 | ✅ test_confirmed_at_is_timezone_aware (datetime.now(timezone.utc)) |
| 86 | 06-13 | (bounty G16) /request-otp had no per-destination throttle → SMS-bomb a victim + burn MSG91 credits | rate limit keyed by IP/user only | 06-13 bounty-r5 | ✅ test_otp_send_throttled_per_destination (60s per-dest send cooldown when provider wired; dev echo unaffected) |
| 87 | 06-13 | (bounty G17) over-long walk-in patient_name/complaint → DB error → generic 500 | no max_length on the Pydantic model | 06-13 bounty-r5 | ✅ test_walkin_overlong_name_is_422 |
| 88 | 06-13 | (TD-019/025/G1/G2) Razorpay payments: verify persisted nothing, no webhook, unauthenticated, trusted client org_id | activation half of billing never wired | 06-13 go-live | ✅ test_payments_billing (create-order auth+plan-derived amount+server-set notes; /api/razorpay-webhook HMAC-verified → idempotent org activate + paid BillingCycle, dedupe on razorpay_payment_id) |
| 89 | 06-13 | (TD-027/F6) call minutes written only in shutdown callback → killed worker lost them | best-effort metering, no durable record | 06-13 go-live | ✅ test_metering_durability (CallLog written at call START, finalized at end; finalize_stale_calls job reconciles crash-stranded rows every 30m) |
| 90 | 06-13 | (TD-020) patient self-cancels stored as cancelled_by_clinic → analytics conflation + wrong rebook framing | only one cancel enum value existed | 06-13 go-live | ✅ test_patient_cancel..cancelled_by_patient (enum value + migration j6cancelpatient2026; _do_cancel→cancelled_by_patient; analytics counts both; rebook filters clinic-only) |
| 91 | 06-13 | (TD-023) changing a doctor's calendar_id orphaned the old recurring event + PATCH-404 on the new | upsert reused stale event id against new calendar | 06-13 go-live | ✅ test_patch_doctor_calendar_change_moves_recurring_event (delete old event from old cal, create fresh on new) |
| 92 | 06-13 | (no-voice-recording) prod could record if RECORDING_ENABLED left true | raw flag read directly; no prod hard-gate | 06-13 go-live | ✅ test_recording_hard_off_in_production (recording_allowed property = false in production regardless; all consumers routed through it) |
| 93 | 06-13 | (TD-014) Docker images ran as root | no USER directive | 06-13 go-live | ⚠ non-root appuser (uid 10001) in both Dockerfiles |
| 94 | 06-13 | (G15 partial) CSP missing frame-ancestors; allowed http sub-resources | spec §10.5 CSP predated frame-ancestors | 06-13 go-live | ⚠ frame-ancestors 'none' + upgrade-insecure-requests added (img-src/style-src tightening deferred — needs frontend render check) |
| 95 | 06-13 | (PII) registration form leaked Vinay's real phone as the placeholder | hardcoded "8096007554" in Register.jsx | 06-13 testfix | ⚠ placeholder → "98765 43210" (source; Cloudflare rebuilds dist on deploy) |
| 96 | 06-13 | signup accepted reserved example/test email domains (xyz@example.com) | normalize_email only checked syntax | 06-13 testfix | ✅ test_invalid_emails_rejected (reject RFC-2606 example.com/test.com etc.) |
| 97 | 06-13 | agent announced a token number for an APPOINTMENT (schedule) doctor | rule existed but booking_type→behaviour mapping was loose | 06-13 testfix | ✅ test_system_prompt_has_availability_grounding_and_name_readback (per-doctor "booking: appointment" drives token-vs-time; explicit no-token rule) |
| 98 | 06-13 | agent fabricated doctor hours ("9-1 and 4-6" for a 9-5 doctor) | no grounding rule; example times in prompt invited parroting | 06-13 testfix | ✅ same guard (AVAILABILITY GROUNDING: state ONLY check_availability output; never invent hours/breaks; examples are format-only) |
| 99 | 06-13 | STT appended to spoken name ("Vinay" → "Vinay Sesh") | no name read-back/confirm step | 06-13 testfix | ✅ same guard (NAME READ-BACK rule — confirm name, never add an unsaid surname) |
| 100 | 06-13 | dashboard showed 0 voice minutes after calls | metering written only at shutdown (FIXLOG #89 now durable) | 06-13 testfix | ✅ test_minutes_logging (finalized CallLog → /analytics/overview minutes.used; start-row+finalize+reconcile job make it durable) |
| 101 | 06-14 | signup required EMAIL OTP — extra friction; Vinay wants mobile-only verification | register gate challenged both sms + email channels | 06-14 productchange | ✅ test_register_needs_no_email_otp + test_register_blocked_without_otp (phone-only gate; email is login identity, never OTP-challenged; Google still skips OTP) |
| 102 | 06-14 | appointment doctor available all day, agent falsely blocked some times; cancelled slot would not free | SLOT hold TTL was (appointment_time − now) + 2h → a hold leaked by a dropped/failed call survived for hours/days, reading booked=1 with 0 confirmed (false block); a real cancel could not pull occupancy below the leaked count | 06-14 livefix | ✅ test_slot_hold_ttl_is_bounded_not_until_appointment (hold TTL fixed at SLOT_HOLD_TTL_SECONDS=900; DB confirmed rows remain authoritative, so a leak self-heals in ≤15 min instead of blocking until the appointment) |
| 103 | 06-14 | agent spoke a token/queue number for an APPOINTMENT (schedule) doctor | prompt rule alone (FIXLOG #97) was ignored; the slot result still surfaced token_number with no machine-readable directive | 06-14 livefix | ✅ test_confirm_booking_announces_time_only_for_appointment_doctor + ..._token_number_for_token_doctor (confirm_booking returns announce=token_number|time_only + explicit instruction; prompt step 7 obeys it) |
| 104 | 06-14 | agent asked "have you booked with same number?" on NEW bookings; reschedule flow confusing | no explicit NEW-vs-EXISTING intent gate; reschedule steps under-specified | 06-14 livefix | ⚠ prompt INTENT GATE (NEW → never find_my_bookings / never ask about past bookings; EXISTING → reschedule/cancel only) + strict 3-step RESCHEDULE (find→ask→reschedule_booking, never hand-roll). Covered by existing reschedule atomicity tests |
| 105 | 06-14 | (feature) inbound caller not identified — returning patients re-asked everything; no "you already have a booking" prompt | greeting + flow were identical for new and returning callers | 06-14 feature | ✅ test_caller_identification.py (caller looked up by number BEFORE greeting; returning patient greeted by name; future bookings injected into prompt; new-booking-while-existing → reschedule-or-new prompt; family-shared number never reveals one name) |
| 106 | 06-14 | (iter1 #14) render.yaml JWT_EXPIRE_HOURS="24" regressed the 8h auth contract (FIXLOG #79) | infra env value drifted from the config default (8) — no guard tied the two together | 06-14 secfix | ✅ test_render_yaml_jwt_expire_matches_config_default (render.yaml value parsed and asserted == Settings().jwt_expire_hours == 8) |
| 107 | 06-14 | (iter1 #2; closes #1/#3/#7/#8/#9/#10/#28/#33/#40, moots #32) dead Pipecat/Vobiz path shipped: Dockerfile ran `uvicorn agent.server:app` (the real brain is the LiveKit worker); fly.toml exposed a phantom inbound port 8080; untracked .mp3 call recordings = unretained patient PHI | the retired Pipecat prototype was never deleted after the LiveKit migration; Docker/fly configs still pointed at it | 06-14 secfix | ✅ test_dockerfile_lint.py inverted (worker CMD present, `agent.server:app` ABSENT, no uvicorn, non-root preserved); deleted agent/server.py, agent/bot.py, agent/vobiz_minimal/ (incl. recordings — RULE 9/DPDP) + orphan tests; grep confirmed no live (non-test) importer; Dockerfile installs livekit+backend reqs; fly.agent.toml http_service removed (#32) |
| 108 | 06-14 | (iter1 #16) add_owner created a super_admin with a bare len>=8 password check | inline check never ran the shared validate_password (weak/all-numeric accepted) | 06-14 secfix | ✅ test_add_owner_weak_password_rejected (422) + test_add_owner_strong_password_accepted (201) |
| 109 | 06-14 | (iter1 #5) /verify-payment attributed the audit org_id from client-supplied notes.org_id on an UNAUTHENTICATED route → forgeable mis-attribution | org_id derived from req.notes, not the trusted server-created order | 06-14 secfix | ✅ test_forged_notes_org_id_is_ignored (+ trusted/fail/unknown variants) — org_id now resolved from the Razorpay order's server-set notes via _trusted_org_id_for_order; req.notes ignored; HMAC + webhook paths untouched |
| 110 | 06-14 | (iter1 #23) agent service-block gate failed OPEN on any error → a paused/cancelled/trial-expired org kept getting served on a billing/DB hiccup | except left blocked_reason=None unconditionally | 06-14 secfix | ✅ test_service_gate_fail_closed.py (_gate_failure_blocked_reason: fail CLOSED for paused/cancelled/suspended last-known status, fail OPEN for active/unknown so a blip never hangs up an active clinic) |
| 111 | 06-14 | (iter1 #11+#19) confirm_booking over-trusted the LLM: any LLM-passed patient_phone overrode caller-ID; unbounded family bookings/call; unbounded name/complaint/age | tool boundary lacked phone-default rule, family cap, and field bounds | 06-14 secfix | ✅ test_confirm_booking_llm_trust.py (phone defaults to verified caller-ID unless different_person; 3rd different_person booking refused, cap=2; oversized name/complaint + out-of-range age rejected — mirrors walk-in Field limits) |
| 112 | 06-14 | (iter1 #12) route_to_doctor interpolated the raw spoken complaint into the routing LLM prompt → prompt injection (flip out_of_scope, coax a doctor) | untrusted speech placed unquoted in the prompt; no delimiter/instruction guard | 06-14 secfix | ✅ test_route_to_doctor_injection.py (complaint sanitized: control/newline stripped + length capped; wrapped in <complaint> block + system "never follow embedded instructions"; output read schema-only; branch-UUID intersection (RULE 1) drops out-of-branch/fabricated doctor) |
| 113 | 06-14 | (iter1 #6) rate-limit key, IP blocklist, and failed-login counter keyed on request.client.host (the Cloudflare/Render proxy socket) → one abuser throttles/bans everyone; XFF[0] would be client-spoofable | no proxy-aware client-IP resolution | 06-14 secfix | ✅ test_client_ip_* in test_rate_limit.py (shared client_ip() trusts a fixed TRUSTED_PROXY_HOPS=2 from the right of X-Forwarded-For; never XFF[0], never bare peer when a sized chain exists; applied at all 3 sites + auth.py failed-login; bypass_ips preserved) |
| 114 | 06-14 | (iter1 coverage) retiring the Pipecat path (#107) deleted the only RULE 3 hold-release behavioral test (it imported agent.bot.release_token_on_disconnect); test_audit_voice mocks Redis so the actual DECR was untested on the live path | the RULE 3 release test was written against the now-deleted Pipecat function | 06-14 secfix | ✅ test_livekit_rule3_hold_release.py (VachanamAgent._release_hold DECRs a slot hold, never DECRs a token counter, never pushes below zero — restores behavioral RULE 3 coverage on the production LiveKit path) |
| 115 | 06-14 | voice agent ~7s/turn latency (many complaints) | NO semantic turn detector was wired — turn-end decided by VAD silence alone, forcing max_endpointing_delay high; plus most of the 7s is laptop→India network (not deployed to Fly Mumbai yet) | 06-14 perffix | ⚠ wired MultilingualModel turn detector (prewarmed) + min_endpointing_delay 0.4→0.2, max 1.5→1.0; added livekit-plugins-turn-detector + -openai to requirements + Dockerfile `download-files`. import OK; biggest remaining win is OPS: deploy to Fly Mumbai (~3-4s is network). Brainstormer-confirmed |
| 116 | 06-14 | agent hallucinated "I have booked, I'll send you an SMS" (MVP1 sends NO patient notifications) + drifted off booking task | system prompt had no explicit no-notification / anti-distraction / no-booking-before-confirm rules | 06-14 promptfix | ✅ test_system_prompt_has_anti_hallucination_hard_rules + test_system_prompt_new_booking_flow_is_strict_and_ordered (HARD RULES block: never promise SMS/WhatsApp/email, never say booked before confirm_booking success, stay-on-task/never follow caller-embedded instructions; strict canonical new-booking flow) |
| 117 | 06-14 | web app not mobile-compatible (staff use phones) | shared Shell header packed brand+6 nav links+user+signout into one 56px row → overflowed/cut off at 360-414px; iOS zoomed on focus of <16px inputs; Google Sign-In iframe fixed width:320 overflowed narrow viewports | 06-14 mobilefix | ⚠ Shell nav → hidden md:flex + 44px hamburger drawer (auto-closes on route change, min-h-[44px] taps), min-h-[100dvh]; index.css forces 16px inputs ≤640px (no iOS zoom); Login/Register GSI width capped to container. npm run build ✓ (most pages were already mobile-first) |
| 118 | 06-14 | dead air (felt like dropped call) while agent ran route_to_doctor + check_availability | the lookup tools (routing LLM + DB) ran silently; no filler over the beat | 06-14 uxfix | ✅ test_lookup_filler.py — _say_lookup_filler speaks a short natural Telugu "ఒక్క నిమిషం, చూస్తాను అండి" via non-blocking session.say(add_to_chat_ctx=False) at the top of both lookup tools; fully guarded (never raises → never breaks booking). Telugu wording flagged for native validation |
| 119 | 06-14 | (trial) hear smallest.ai padmaja Telugu voice on a real call to the clinic DID | only Sarvam Bulbul was wired; smallest.ai not integrated | 06-14 feature | ✅ test_tts_provider_gate.py — opt-in env gate _build_tts: default sarvam (prod untouched), TTS_PROVIDER=smallest+SMALLEST_API_KEY → smallestai.TTS(voice padmaja, model lightning_v3.1); fail-safe fallback to sarvam if key missing/plugin load fails (RULE 8). padmaja confirmed working in sandbox (Telugu WAVs) |
| 120 | 06-14 | sarvam.TTS(min_buffer_size=10) raises ValueError "must be between 30 and 200" → default TTS path crashes every call | livekit-plugins-sarvam (1.5.17) tightened the valid range; the long-standing value 10 is now out of range | 06-14 fix | ✅ caught while verifying the smallest.ai gate; min_buffer_size 10→30 (smallest valid = fastest first-audio). _build_tts now constructs the sarvam default cleanly |
| 121 | 06-15 | robotic Telugu in fixed utterances + prompt examples | literary/stiff phrasing; A1 formal vs A2 casual greeting inconsistency | 06-15 promptfix | ✅ applied Vinay's natural Telugu for all 13 hardcoded utterances + 23 prompt examples (agent.py + system_prompt.py); fixed 2 paste corruptions (B6 Devanagari समस्या→సమస్య, B7 Japanese 検→క్ష); no stray non-Telugu scripts (scan clean), import OK, 42 prompt/filler/tts tests pass |
| 122 | 06-15 | remove smallest.ai TTS trial; revert to sarvam-only (Vinay) | smallest.ai Waves trial (FIXLOG #119) no longer wanted | 06-15 revert | ✅ deleted _build_tts gate (call site inlines sarvam.TTS, keeps min_buffer_size=30 from #120); dropped tts_provider/smallest_* from config.py + .env.example + local .env files; removed livekit-plugins-smallestai from requirements; deleted agent/sandbox_smallest_tts/ + test_tts_provider_gate.py. agent imports OK (_build_tts gone), config has no smallest attrs, 406 passed (2 pre-existing seed-env failures unrelated: VOBIZ_DID_NUMBER commented out in .env) |
| 123 | 06-15 | signup reworked: email + password + EMAIL OTP, mobile dropped (Vinay, reverses 06-14 mobile-only) | new design (image) is email-first; phone not collected | 06-15 feat | ✅ /register email-OTP gate (email_ok via verify_code/is_verified("email")), owner_phone seeded "" + filled later in Settings; Register.jsx "Create an account" + live password checklist; validate_password now needs lower+upper+digit+special. test_register_otp (email-only) + test_validators + staff/owner fixtures updated; 65 auth tests pass, frontend build clean |
| 124 | 06-15 | market repositioned Hyderabad → all-India (Vinay) | product copy said "Hyderabad clinics" | 06-15 brand | ⚠ landing/static + React Login/Landing/Settings + CLAUDE.md/MAIN_AGENDA + agent style note changed to India; legal governing-law jurisdiction (Hyderabad courts) + founder address KEPT intentionally (legal, not marketing). No behavioural code path; build clean |
| 125 | 06-15 | multilingual voice agent: per-clinic language (Vinay) — Telugu + 7 more | agent was Telugu-only across STT/TTS/prompt/spoken lines | 06-15 feat | ✅ new agent/i18n (registry + per-language spoken lines, 8 langs); Branch.language col + migration l8lang2026 (default te); agent.py drives Sarvam STT/TTS codes + greetings/fillers/caps/brevity per Branch.language; build_system_prompt(language=) adds a PRIMARY-LANGUAGE directive (Telugu byte-identical, no regression); Settings language dropdown + /voice PATCH persist + validation. test_multilingual (script-purity + te-fallback + prompt) + settings E2E lang assertion; 440 passed. ⚠ ta/kn/ml/mr/bn/or are FIRST-PASS, flagged for native review (docs/multilingual_lines_review.md); reminder/rebook date/time render in Telugu words only for te, plain format otherwise |
| 126 | 06-15 | per-clinic Vobiz sub-accounts for concurrency isolation (Vinay) — credential seam | one global Vobiz account shared channel pool/CDRs/billing across all clinics; busy clinic starves others | 06-15 feat | ✅ Branch.vobiz_subaccount_id/sip_username/sip_password_enc/sip_domain/outbound_trunk_id (migration m9subacct2026, all nullable → single-account branches unchanged); backend/services/crypto.py (Fernet field encryption, key from FIELD_ENCRYPTION_KEY or derived from jwt_secret in dev); backend/services/telephony.py resolver (per-branch creds+trunk, else global fallback, decrypt-failure falls back not crash); agent.py outbound dial + reminder/rebook jobs stamp/use per-clinic outbound_trunk_id from dispatch metadata; PATCH/GET /branches/{id}/telephony (org_admin, password encrypted at rest + never echoed). test_telephony_seam (crypto round-trip/tamper/fallback) + settings E2E (ciphertext-at-rest, no secret echo); 446 passed. ⚠ Vobiz-API auto-provisioning of sub-accounts deferred (Vobiz partner-API capability TBD — see TECH_DEBT); creds entered manually for now |
| 127 | 06-15 | replace Sarvam TTS with smallest.ai + voice cloning (Vinay; reverses the same-day sarvam-only revert) | TTS provider switch to smallest.ai Waves; clinics want voice cloning | 06-15 feat | ✅ followed official docs (https://docs.smallest.ai/waves) + live-probed the API. STT stays Sarvam Saaras; TTS → smallest.ai Lightning v3.1 via livekit-plugins-smallestai (gate+main sessions). smallest language codes == our Branch.language codes (en/hi/mr/kn/ta/bn/gu/te/ml/pa/or — all 8 of ours incl Bengali+Odia verified via get_voices, 241 voices). i18n LangConfig.tts_code→short code + default_voice per language (padmaja te/ta/kn/ml, niharika hi/mr/bn/or — both confirmed in live catalog). Branch.tts_voice → nullable smallest voice_id (migration n10smallestvoice2026; old 'rupali' sarvam default reset to NULL→language default). backend/services/smallest_voice.py (SDK: list_voices [live-proven], clone_voice, delete_cloned_voice); GET /branches/{id}/voices catalog + POST/DELETE /voice-clone (org_admin, 10MB cap, cloned voice_id stored per-branch=tenant-scoped). Settings UI: catalog voice picker + clone-upload form. test_smallest_voice (mock SDK) + test_multilingual (codes) + settings E2E updated; 445 passed. ⚠ live cloning UNVERIFIED — the SDK's add/delete target the deprecated lightning-large path (410 on probe); integrated against the official SDK method + surfaced clean errors; confirm the current clone endpoint/plan before relying on it (TD-027). Rotate the smallest key (pasted in chat) |
| 128 | 06-16 | smallest key returned 401 on synthesis → agent TTS would fail every call | the old key had catalog-read access only (no synthesis/credits) | 06-16 fix+feat | ✅ new pay-as-you-go key: `POST /waves/v1/tts` → 200 valid WAV (agent TTS unblocked, proven). Landing "Agent voice" reworked into a LANGUAGE showcase: generated real smallest.ai samples of the agent greeting in all 8 languages (default voice per language) → frontend/public/voices/lang/{te,hi,ta,kn,ml,mr,bn,or}.wav (8/8 valid RIFF); VoicePicker plays them (native+English labels); Landing copy "Eight languages, one warm AI agent". Removed the 6 orphan Sarvam sample wavs. frontend build clean. Voice cloning remains endpoint-blocked (TD-027) — it's an opt-in sell-feature, default voices cover all clinics |
| 129 | 06-16 | use a clinic's CLONED voice on calls + make it selectable (Vinay's te clone voice_euTAvxhJLE) | cloned voices work in synthesis but weren't usable/selectable (in-app upload-create endpoint still blocked, TD-027) | 06-16 feat | ✅ proved a cloned voice_id synthesizes (voice_euTAvxhJLE → te → 200 WAV) → the agent CAN speak in it. Branch.cloned_voices JSONB (migration o11clonedvoices2026): per-clinic registry of dashboard-cloned voices [{voice_id,name,language}], tenant-scoped. GET /voices merges the branch's cloned voices (language-matched, cloned=true) above the catalog so they show in the picker. POST/DELETE /branches/{id}/cloned-voices register/remove by id (org_admin) + set_current. Settings UI: paste-id "Add a cloned voice" form (replaces the blocked upload form) + remove buttons; picker tags "your cloned voice". Set Vinay's branch (DID +918046733493) live: tts_voice=voice_euTAvxhJLE, registered as "Vinay" te → agent speaks in it on calls (DB-verified: picker shows Vinay first + selected). settings E2E extended (register→merge→remove); 446 pass. Alembic head o11clonedvoices2026 |
| 130 | 06-16 | agent spoke a TOKEN/queue number on SCHEDULE (appointment) doctor bookings (Vinay live, recurring; #97/#103/#104 prior) | the assign_token TOOL returned token_number (an internal slot index) to the LLM for appointment doctors; prompt rules alone kept being ignored, so the model spoke the number | 06-16 fix | OK. Root-cause close at the data layer: the agent assign_token wrapper now NEVER puts a number in front of the model for appointment doctors -- it returns {announce:"time_only", appointment_time, instruction:"confirm only date+time, never a token number"}; the real index stays server-side in state.token_number for confirm_booking. assign_token core returns booking_type so the wrapper can branch. Token (walk-in) doctors still surface the queue number (it IS the patient answer). test_assign_token_wrapper_hides_queue_number_for_appointment_doctor + _keeps_..._for_token_doctor; 449 pass |
| 131 | 06-16 | cancelled/rescheduled TOKEN booking did NOT free its seat -- the day stayed "full", couldn't rebook (Vinay live, raised twice) | capacity was gated on the MONOTONIC number counter (`token_number > limit` / max(redis,db)); the counter never decrements on cancel (#24 unique-number rule), so every cancellation permanently ate a seat | 06-16 fix | OK. capacity is now the CONFIRMED-seat count (rows with status='confirmed'), which a cancel/reschedule drops out of automatically -> seat freed, day rebookable. Numbers stay monotonic & unique (counter still INCR-only, never reused -- #24 intact); assign's seat check is advisory, confirm_booking's confirmed-count re-check (#48/#67) stays the race-authoritative gate (RULE 2 no-double-booking core = unique numbers, still atomic). assign_token + check_availability token paths reworked. test_cancelled_token_frees_seat_for_rebooking (confirm 2/limit-2 -> assign full -> cancel 1 -> rebook ok, number=3 never reused); updated 3 legacy counter-cap tests to the seat invariant (booking_flow x2 + concurrent boundary now asserts unique-numbers-under-concurrency); 449 pass. Note: daily_token_limit can be exceeded by a few only under exact-simultaneous CONFIRMS (queue soft-cap, NOT a double-booking) -- atomic seat-key deferred (TD-026) |
| 132 | 06-16 | email OTP only dev-echoed — no real verification email sent (Vinay) | no email provider wired; `_send_email` SMTP path gated on `smtp_host`, unset → code logged/echoed | 06-16 feat | OK. Resend HTTP sender added (chosen provider): `_send_email_resend` POSTs `https://api.resend.com/emails` (Bearer `RESEND_API_KEY`, from `RESEND_FROM`=Vachanam <noreply@vachanam.in>); `_send_email` prefers Resend over SMTP; `_provider_configured("email")` now true when resend key OR smtp_host set → OTP stops dev-echoing once keyed (fail-closed F3 intact). config `resend_api_key`/`resend_from` + .env.example. test_email_provider_configured_via_resend + _send_email_uses_resend_when_keyed (mock httpx: correct URL/Bearer/to, code in text not subject); 454 pass. Prod: set RESEND_API_KEY + verify vachanam.in domain in Resend |
| 133 | 06-16 | agent talks to itself / echo loop on calls (Vinay live) | carrier/SIP line echo bounces the agent's TTS back; Sarvam STT transcribes it as the caller → agent answers its own words → loop (BVCTelephony AEC doesn't fully cancel carrier echo) | 06-16 fix | OK. ECHO GUARD in VachanamAgent.on_user_turn_completed: drops a user turn that is a near-verbatim echo of the agent's immediately-preceding utterance (difflib ratio>=0.85 OR a >=25-char containment, both over normalized text). Strict thresholds (>=20 chars, ~85%) so a REAL patient turn is never silenced — false-negative (rare echo slips) chosen over false-positive (ignoring caller); StopResponse discards the turn; any guard error lets the turn through. test_echo_guard_discards_agents_own_words + _keeps_real_patient_turn (distinct long turn + short "సరే అండి" both pass through); 454 pass. ALSO prompt #3: TTS-DIRECT spoken-style block ("every char goes straight to TTS, speak like a real human, no markdown/asterisks/lists/emoji, vary wording") + per-language directive made natural-human + TTS-direct for all 8 languages (was Telugu-only style + generic). Needs agent RESTART + a live-call check to confirm the loop is gone |
| 134 | 06-16 | sub-processor disclosure FACTUALLY WRONG (DPDP) — undisclosed processors of personal data (Vinay compliance audit) | TTS switched to smallest.ai (#127) + Resend added for email OTP, but DPA s4 + privacy-policy s6 still said Sarvam did TTS and never listed smallest.ai/Resend; the DPA's own s4.1 commits to accurate disclosure | 06-16 fix | OK. DPA s4 + privacy-policy s6 updated: Sarvam=STT-only, +smallest.ai (TTS, receives patient name, Global), +Resend (staff email OTP, Global/US); privacy-policy s11 data-residency line corrected (TTS + LLMs are Global, not all-India). DPDP s.16 cross-border was already disclosed for Gemini/OpenAI. Doc-only (no code). Verify smallest.ai's real data location; adding processors triggers DPA s4.1 30-day clinic notice once clinics signed |
| 135 | 06-16 | no per-call CONSENT RECORD — notice spoken but not logged, can't demonstrate consent to DPB (DPDP s.5) | verbal disclosure played in the greeting but nothing persisted; no consents table | 06-16 feat | OK. new `consents` table (migration p12consent2026): branch_id (RULE 1) + session_id + patient_phone + consent_type(data_processing|followup|recording) + notice_version + method(verbal|written|whatsapp) + created_at. Agent writes one data_processing row after the inbound disclosure greeting (own short-lived session, fire-and-forget — never breaks a call). test_data_retention.py::test_consent_row_records_notice; 458 pass. Alembic head p12consent2026 |
| 136 | 06-16 | no automated retention/erasure — patient PII kept indefinitely (DPDP s.8(7)) | retention periods stated in privacy policy but never enforced; no job | 06-16 feat | OK. backend/jobs/data_retention.py (daily APScheduler): erases a patient's PII (name->'[erased]', phone/age/gender->NULL, anonymized_at stamped) once they have NO appointment within patient_retention_days (config default 730 = 2yr, matches policy); booking rows survive (anonymised, hold no PII beyond patient_id) for analytics; prunes consents past the window. patients.anonymized_at col added (migration p12consent2026). Registered in main.py lifespan (leader-only). test_data_retention.py: erases stale, keeps recent (a single recent booking keeps the record), prunes old consents; 458 pass |
| 137 | 06-16 | voice cloning was open to ALL plans — make it a Clinic/Multi up-sell (Vinay) | clone endpoints gated on org_admin only, not plan | 06-16 feat | OK. VOICE_CLONE_PLANS=(clinic,multi) + _assert_voice_cloning_plan enforced on POST /cloned-voices AND /voice-clone (Solo -> 403 "available on Clinic and Multi"); BranchSettings.voice_cloning_allowed flag surfaced for the UI; Settings.jsx hides the clone form for Solo and shows an upgrade note. test_voice_cloning_gated_to_clinic_and_multi_plans (solo 403 + flag False; clinic path proven by main E2E); 459 pass, frontend build clean. Also landing: Telugu-only positioning -> multilingual (eyebrow/body/step/Solo line), hero chat-bubbles -> English voice-call transcript w/ speaker labels (was all-Telugu, looked like texting) |
| 138 | 06-17 | ⚠ a render-time crash showed users a blank white screen — no React error boundary (Vinay: "users won't see error messages when something crashes") | front-end had NO ErrorBoundary; any uncaught throw during render unmounts the whole tree -> white screen. Backend already returns generic `{"detail":"Internal server error"}` (main.py:173, no stack leaked) and the agent already speaks a polite line on failure (RULE 8) — gap was front-end only. NOT blanket try/except (rejected: hides bugs, kills stack traces, RULE 10) — a global boundary is the right layer | 06-17 fix | OK. Added components/ErrorBoundary.jsx (class boundary, getDerivedStateFromError + componentDidCatch): logs full error to console only, renders a calm on-brand card ("Something went wrong / your data is safe / reload + hello@vachanam.in") with a Reload action; never shows a stack to the user. Wrapped outermost in main.jsx (above QueryClient/Router/Auth) so provider + route throws are caught too. Brand-exact colors (#008F8F teal, #F0FAFA bg). proof = `npm run build` clean (154 modules, 3.11s). Manual check: throw in a page render -> fallback card, not white screen. [UPDATE #139: now has an automated guard — vitest + RTL added, ErrorBoundary.test.jsx (3 tests: healthy children render, a throwing child shows the fallback + Reload and NOT the raw error, Reload calls location.reload); 3 pass] |
| 139 | 06-17 | agent only handled the cooperative happy-path caller; real callers are angry, abusive, shy, rambling, dial the wrong number, or call on a reference knowing nothing (not even the address) — agent had no guidance for any of these (Vinay) + front-end #138 had no automated guard | system_prompt covered off-task/prompt-injection (rule 5), garbled input, wait requests, human transfer — but NOT mood/state handling, wrong-number, or reference callers; and the clinic ADDRESS (a safe grounded fact) was never passed to the agent, so a "where are you?" caller got nothing | 06-17 fix | OK. (a) build_system_prompt gained a "HANDLING DIFFERENT CALLERS" section: ANGRY (stay soft, never match anger), ABUSE/nonsense (don't repeat/scold; pure-abuse-no-intent across turns -> polite end_call, never retaliate), SHY/slow/elderly (one thing at a time, patient), RAMBLING (extract the booking bit, steer), WRONG NUMBER (clarify clinic, offer or warm goodbye), DOESN'T KNOW THE CLINIC (reassure, say what it treats; address/timings ONLY from given info). (b) clinic_address param threaded build_system_prompt <- agent.py (branch.address): grounded line spoken only if asked; when unset, explicit "do NOT invent an address" (HARD RULE 2). 4 new prompt tests (section present + all personas named, never-retaliate, address grounded when set, not-invented when absent); test_system_prompt 27 pass, test_multilingual 30 pass. (c) #138 automated guard: vitest 2.x + @testing-library/react/jest-dom + jsdom installed, vite.config test block (jsdom/globals/setup), npm test script, ErrorBoundary.test.jsx 3 pass. NOTE latency (#1 of the same request): traced every turn-loop stage — semantic turn detector, preemptive_generation, thinking_budget=0, flush_signal, flash-lite routing, lightning_v3.1 TTS all already optimal; remaining latency is provider TTFT/TTFB + network (data-bound). Per-stage lat_* metrics already logged; further cuts need a real call's numbers, not blind param changes (which would risk cutting patients off) — measurement deferred to a live test call |
| 140 | 06-17 | voice agent slow "checking from DB and getting back" before answering (Vinay) — NOT a schema problem (schema is well-indexed: branch_id everywhere + composite tokens/consents/call_log indexes) | MEASURED: every call runs on a FRESH event loop, so get_loop_engine() hands a COLD pool — the FIRST DB query of each call paid a ~1.8s TLS+auth handshake to Neon (cross-region Singapore), then 5 SERIAL setup queries stacked ~60-100ms RTT each. Cold setup = 2901ms, landing right before the greeting. Root cause = per-call cold connection + serial cross-region round-trips, not table design | 06-17 fix | OK. (1) WARM-POOL prewarm: entrypoint fires `_warm_db_pool()` (get_loop_engine + `SELECT 1`) as a fire-and-forget task BEFORE the ~4s SIP participant-wait, so the TLS handshake overlaps existing dead time and the pool is hot when branch resolution runs. Measured: setup 2901ms cold -> 652ms warm (~2.25s off the pre-greeting path); never blocks/fails a call (best-effort, logged). (2) collapsed the minute-gate branch_ids N+1 (SELECT branch ids then IN) into one in-DB scalar subquery -> one round-trip on the hard-block path. Schema NOT rewritten (was sound). Proof = before/after timing harness + 106 minute-gate/booking/billing tests pass, agent.py compiles+imports. Phase-2 option (deferred): Redis-cache branch config + doctor list keyed by branch_id (RULE 1, non-PII only) to drop the remaining ~3 warm round-trips |
| 141 | 06-17 | "AI tool without monitoring is not a product" + "unimproved app goes to trash" — no per-call quality capture, so no monitoring corpus and no feedback loop (Vinay) | structlog/lat_*/CallLog/CDR existed but only billing-grade; nothing recorded a call's OUTCOME (booked/failed/abandoned/transferred), turns, language, or the conversation transcript needed to score + improve speech/routing/handling | 06-17 feat | OK (step 1 of 3 = capture layer). New `call_quality` table (migration q13callquality2026): agent writes ONE row per call at shutdown REGARDLESS of agent_call_log_enabled (CallLog stays billing-only; this is quality) — branch_id (RULE 1) + call_log_id soft-link (SET NULL) + session_id + call_type + language + duration + turns + booking_made + booking_abandoned + transfer_requested + fail_reason, plus an OPTIONAL `transcript`. Two retention clocks: outcome fields are non-PII (kept for trends); transcript is PII (caller name/age/health complaint — TEXT only, RULE 9 keeps prod recording-free) so it's captured only when settings.transcript_capture_enabled (default on), phone digits MASKED via `_mask_pii_for_transcript` (\d{4,}->[number]), and NULLED by data_retention after settings.transcript_retention_days (default 90) while the outcome row survives. SessionState gained language/transfer_requested/fail_reason; transfer tool + setup set them; `_extract_call_record` builds (patient_turns, masked transcript) from session.history defensively (never raises -> never breaks teardown). Migration applied to Neon (head q13; confirms TD-027 stamp workaround: new migrations apply forward cleanly). Proof: 5 capture-helper unit tests + 1 retention transcript-prune test (nulls old transcript, keeps outcome row + recent transcript), full suite re-run. NOTE: fail_reason currently records abandoned_hold; tool-level reasons (out_of_scope/no_slot) are a follow-up enrichment. Steps 2 (quality metrics on analytics dashboard) + 3 (LLM-as-judge scoring job) next |
| 142 | 06-17 | monitoring step 2: the call_quality capture (#141) had no surface — owners couldn't see how the AI receptionist is performing | data was being written but nowhere visible; no aggregate endpoint, no dashboard card | 06-17 feat | OK (step 2 of 3 = dashboard). New GET /analytics/call-quality (branch_id + days): AGGREGATES ONLY over call_quality — total_calls, booked, conversion_rate, abandoned/abandon_rate, transfers, avg_turns, avg_duration, by_language split, fail_reason breakdown. RULE 1 (every query filters branch_id + assert_branch_access from JWT); NEVER returns transcript text (PII stays internal for the judge job). Frontend: fetchCallQuality in client.js + a "Call quality · Nd" card on the owner Dashboard (QStat tiles: booked %, abandoned %, human-asked, avg turns, avg length + a "why calls didn't book" failure-reason chip row), empty-state until first AI calls. Proof: 2 endpoint tests (aggregation correctness + RULE-1 cross-branch isolation + PII guard asserting transcript text absent from the response + 401/403 without branch access); test_minutes_logging still green (4 backend pass); frontend build clean + 3 vitest pass. Step 3 (LLM-as-judge scoring of transcripts) next |
| 143 | 06-17 | feedback-loop step 3 (LLM-as-judge) + a super_admin realtime monitoring page (Vinay) | captured transcripts were unscored (no automatic quality read); super_admin had Operations but no platform-wide monitoring/feedback view | 06-17 feat | OK (step 3 of 3). (a) JUDGE: call_quality gained judge_score(1-5)/judge_tags(JSON, fixed vocab)/judge_summary(1-line, PII-FREE, internal)/judged_at (migration q14callscore2026, applied to Neon, partial index on unjudged+transcript). backend/jobs/call_scoring.py (hourly APScheduler leader): scores unjudged transcripts via gemini-2.5-flash-lite (thinking_budget=0, JSON), idempotent (judged_at gate), batch, per-row try/except; transcript already flows to Gemini live (disclosed sub-processor) so no new data flow, stored output is non-PII; survives the transcript prune. DPA s4 Gemini row updated (also scores transcripts; output non-identifying). (b) SUPER_ADMIN MONITORING: GET /admin/monitoring (require_admin) — platform-wide AGGREGATES across ALL clinics (calls/conversion/abandon/transfer, avg judge score, issue-tag frequencies, per-clinic rollup by clinic NAME, daily trend). RULE 1: NO patient PII, NO transcript, NO judge_summary cross the boundary. Frontend: Monitoring.jsx at /admin/monitoring (Shell nav link), polls 30s (realtime), KPI tiles + tag bars + daily volume + per-clinic table. (c) HARNESS FIX (uncovered, pre-existing — PROVEN by git-stash: the 4 victim files fail together on CLEAN code too, so NOT introduced here): test engine now uses NullPool so overlapping sessions (fixture + get_db + job) across pytest-asyncio's function-scoped loops never reuse one asyncpg connection (was tripping "another operation is in progress" under certain multi-file orderings; pool_pre_ping alone was insufficient). To stop NullPool's fresh-connection-per-session from exhausting Postgres max_connections, the 100-concurrent-callers token test now gates fan-out with asyncio.Semaphore(25) (still races in waves — atomicity assertions unchanged). /admin/monitoring switched to Depends(get_db); test_org_controls_bad_org_404 given the db fixture (it queried organizations with no schema-creating fixture — latent bug NullPool exposed). Proof: 3 judge-job tests (only-unjudged-with-transcript, idempotent, failure-leaves-unjudged) + 2 monitoring-endpoint tests (aggregation + RULE-1 + judge_summary/transcript NEVER in response + non-admin 403); the previously-failing 5-file chain now 35 pass; FULL SUITE 476 passed / 0 failed (also cleared the long-standing concurrency-token flake); frontend build clean + 3 vitest pass |
| 144 | 06-17 | mid-call language switch half-failed — Tamil agent asked to switch to Hindi spoke a few Hindi words then drifted back to Tamil (Vinay live) | three locked layers: STT pinned to the clinic code, TTS voice pinned (a Dravidian voice cannot pronounce Hindi), and the prompt said "speak {lang} ONLY" | 06-17 investigated → KEEP STRICT (Vinay's call) | OUTCOME: strict single language per call (no mid-call switch). Investigated a switch feature thoroughly: built + tested an explicit-ask switch (switch_language tool hot-swapping TTS via update_options + a Hindi-capable voice, strong prompt directive) AND tried STT auto-detect (saaras:v3 language="unknown", 23 langs). REVERTED BOTH, because: (1) Vinay flagged auto-detect mis-infers language on shared words ("amma"/numbers identical across te/ta/kn) → wrong transcription every call; (2) DECISIVE — LiveKit `session.stt` is a READ-ONLY property and `session.update_options` covers only endpointing, so the STT CANNOT be hot-swapped mid-call; an explicit switch could change the spoken language (TTS) but NOT what the agent can UNDERSTAND, so the caller's new-language replies would still hit the old STT → half-broken, worse than strict. A truly working switch needs a heavier `session.update_agent` handoff (new Agent w/ new STT) — deferred. Per Vinay's own criterion ("if it causes discrepancies, ok to stick to strict") we keep strict. Net change vs prod: STT comment documents the decision; no behaviour change. If revisited, the path is agent-handoff, not auto-detect |
| 145 | 06-18 | agent Fly deploy (vachanam-agent, bom) failed 3×, worker never registered with LiveKit (Vinay go-live) | THREE deploy-config bugs, each surfaced only at a later build/run stage: (1) `fly.agent.toml` `[build].dockerfile = "infra/Dockerfile.agent"` is resolved RELATIVE TO THE CONFIG FILE's dir (infra/), so flyctl looked for `infra/infra/Dockerfile.agent` → "dockerfile not found". (2) the BUILD-ONLY ENV dummies in Dockerfile.agent (needed because `download-files` imports backend.config → `Settings()` whose required fields have no defaults, and .env is .dockerignore'd out of the image) were MISSING `SARVAM_API_KEY` — config requires sarvam/openai/gemini/database_url/redis_url/jwt_secret, only 5 were dummied → pydantic `ValidationError: sarvam_api_key Field required` at the `download-files` layer. (3) the 1024MB VM OOM-killed the LiveKit-Agents-1.6 inference-executor CHILD process during model preload (silero VAD + multilingual turn-detector + warmup run in a SEPARATE process from the main worker) → `Out of memory: Killed process 669 ... anon-rss:536068kB`, then `DuplexClosed`/`IncompleteReadError` on the IPC channel and `worker failed` before registration; machines then crash-looped to `stopped` | 06-18 fix | OK. (1) toml dockerfile → `Dockerfile.agent` (relative to infra/); build context stays CWD=repo-root so `COPY agent/`/`COPY backend/` still resolve. (2) added `SARVAM_API_KEY=build` to the Dockerfile build-only ENV block (all 6 required config fields now dummied for the download-files step; Fly runtime secrets override). (3) `[[vm]].memory_mb` 1024 → 2048 (commented why: separate inference-executor process). Pre-deploy also: stripped BOM from .env + agent/.env (UTF-8 no-BOM) so `fly secrets set` accepted key names (PowerShell pipe-to-stdin had been prepending ﻿ to the first key); staged GOOGLE_SA_JSON_B64 (base64 of the SA file — NOT in image, .dockerignore'd; agent decodes to /tmp at boot, RULE 4 calendar write needs it) + APP_ENV=production. PROOF (infra config, no unit test): clean `fly deploy` exit 0, image 912MB, then logs show `process initialized (inference: true, elapsed 73.35s)` (no OOM on 2GB) and `registered worker id=AW_yPvfH2ZE7FYz agent_name=vachanam-agent url=wss://vachanam-voice-lh0wwtps.livekit.cloud region="India West"`. Remaining go-live (not code): Vobiz DID→LiveKit SIP inbound trunk + dispatch rule → agent, and one clinic row with that DID |
| 146 | 06-21 | dead air after pickup before the agent spoke — outbound worst (Vinay: "5 seconds silence") | TWO causes, found by live diagnostic calls + per-stage `lat_*` logs: (1) nothing reached the caller until the full AgentSession was live (branch resolve + `session.start` connect ~3s ran before the first `session.say`); (2) for OUTBOUND the DB pool-warm was created AFTER the dial, so branch resolution (run the instant the patient answers) paid the cold ~1.8s Neon TLS handshake every call | 06-21 fix | ✅ test_welcome_audio. (a) INSTANT CLIP: `agent/livekit_minimal/welcome_audio.py::play_welcome` synths a SHORT per-clinic "namaskaram <clinic> clinic ki swagatham" (i18n `get_welcome`, te Vinay-validated + 7 first-pass) and publishes it into the room the moment the branch resolves — kicked off concurrently with setup, awaited+unpublished BEFORE the AgentSession is built (so it plays out on a free loop; MultilingualModel() ctor can briefly block). Inbound AND outbound; the REAL greeting (reminder/rebook/disclosure) still plays AFTER session.start so STT is live and the patient's reply is heard. AudioSource is built from the first frame's OWN sample_rate/channels (a declared-rate mismatch had stretched playout). DECISIONS proven wrong en route + corrected: playing the FULL personalized greeting as the clip made it 11s long and blocked STT until done (lat_setup 15s) → reverted to short bridge; the AgentSession ctor was NOT the 13s (lat_agentsession_ctor=0.25s) — the await of a stretched-playout clip was. (b) moved `_warm_db_pool` ABOVE the outbound dial so the pool warms during ring (dead time). Best-effort throughout: any clip failure swallowed, greeting still plays (RULE 8). PROOF: 4/4 unit tests (welcome text + te fallback; captures frames, returns True, lazily publishes + always unpublishes; on TTS failure returns False without raising & never publishes). Per-stage `lat_*` + `welcome_clip_first_frame/done` logs added for future tuning |
| 147 | 06-22 | Settings voice picker showed too many voices (Vinay) | `list_voices` returned the FULL smallest.ai catalog for the language — long, noisy list | 06-22 fix | ✅ test_smallest_voice (`_select_top`). Trim to 5: 3 female + 2 male, via pure `backend/services/smallest_voice.py::_select_top(voices, default_id)`. The language's default voice (get_lang(code).default_voice, e.g. padmaja/niharika) is pulled to the front of its gender bucket so it's always offered; genderless tags excluded; short buckets just yield fewer (no padding). Backend-only — the Settings list renders whatever the `/branches/{id}/voices` endpoint returns. PROOF: 2 unit tests (caps 3F/2M + default-first + excludes genderless; short buckets yield 1F+1M). Pre-existing test_clone_voice_returns_voice_id failure is unrelated (live smallest.ai 500) |
| 148 | 06-22 | Render free-tier backend slept (15min idle) → in-process APScheduler died → reminders/jobs stopped firing (Vinay) | Free Render sleeps after ~15min idle; the only keepalive was a GitHub Actions cron (`keepalive.yml`, */10) which drifts 10-30min under load and lost the race — Render found asleep (42s cold /health). UptimeRobot was never set up: it needs a monitor created in their dashboard with Vinay's account (can't be done from code) | 06-22 fix | ⚠ manual (live-log proof). The always-on Fly agent now self-pings Render: `_start_render_keepalive()` (daemon thread, stdlib urllib) hits `BACKEND_HEALTH_URL` (default Render /health) every 5min (3× margin on the 15min sleep). Started in the `__main__` block — NOT `_prewarm` (prewarm runs in the job subprocess which may not spawn until the first call, and Render sleeps exactly when there are NO calls). Best-effort: a failed ping only logs `render_keepalive_ping_failed`, never touches the call path; set BACKEND_HEALTH_URL empty to disable. PROOF: agent log `render_keepalive_started url=...onrender.com/health interval=300s` on boot, then Render /health 0.68s warm (was 42s asleep), zero ping failures. (GitHub Actions keepalive.yml left as a weak backup.) |
| 149 | 06-22 | patient got NO 30-min reminder call for a 16:30 appointment (Vinay live); token qualified (confirmed, reminder_sent=False, doctor reminder ON, booking_type=appointment, phone present) yet was never processed in its window | the reminder window was only 3 min wide (28-31 min before). The in-process APScheduler on Render free tier must tick during that exact 3-min band; a single missed tick (Render restart/gap/slow job) moved the band past the appointment, so `reminder_sent` never flipped and the reminder was dropped PERMANENTLY. Render had LiveKit creds (voice_plane ok) — the band, not config, was the bug | 06-22 fix | ✅ test_reminder_window_resilient. `reminder_window` lo changed `now+28` → `now` (band now [now, now+31min]); fires on the FIRST tick inside it, so a missed 30-min mark catches up (patient gets a slightly-later reminder instead of none). Past appointments (appt<now) still excluded; midnight no-wrap preserved (datetimes). PROOF: 6 unit tests (fires at 30-min; catches up at 5-min after missed ticks; fires up to appt time; waits if 45-min out; excludes past; no midnight wrap). Immediate: fired the live 16:30 reminder via the real dispatch path (scripts/_fire_real_reminder.py) |
| 150 | 06-22 | agent skipped the sentence it was saying when the caller spoke mid-utterance (Vinay live) — half-said confirmations (token/doctor/time) lost | Telugu/Indian callers backchannel while the agent speaks ("haan"/"ఊ"/"సరే"/"mm"); LiveKit's default barge-in treats a single such sound as a real interruption → truncates the agent's turn AND the LLM moves on, never re-saying the cut content | 06-22 fix | ⚠ manual (live call) + param-accept test. AgentSession (agent/livekit_minimal/agent.py main call session) now sets `min_interruption_words=2` + `min_interruption_duration=0.6` (a lone backchannel word/short sound no longer interrupts) and `resume_false_interruption=True` + `false_interruption_timeout=2.0` (a false interruption RESUMES the cut sentence instead of skipping it; a genuine multi-word interruption still stops the agent). PROOF: params verified accepted by livekit-agents 1.6 AgentSession signature + agent.py parses; live-call confirmation pending Vinay's test |
| 151 | 06-22 | reminders STILL missing after the window fix (#149) — Vinay got no call though the job ran (token reminder_sent=True, no dial) | the job set `reminder_sent=True` BEFORE dialing, so ANY create_dispatch failure (a Render LiveKit hiccup/transient) permanently suppressed the reminder: the flag was already True, every later tick skipped it, the call never came. Flip-before-dispatch turned a transient dispatch error into a permanent missed reminder | 06-22 fix | ✅ test_reminder_flip_after_dispatch. `_dispatch_reminder_call` now returns bool; the loop dispatches FIRST and marks `reminder_sent=True` ONLY when dispatch succeeds — on failure the flag stays False so the next tick retries within the resilient [now, now+31] window (self-healing). Also added `GET /health/voice-plane` (booleans + a LiveKit reachability probe, NO secrets) so the Render host's dispatch capability is provable without log access. PROOF: 2 unit tests (failed dispatch leaves reminder_sent False for retry; successful dispatch marks True + dispatch attempted) + 6 window tests green; plus a live /health/voice-plane probe of the deployed Render host |
| 152 | 06-22 | ROOT CAUSE of missing reminders: Render backend threw "No module named 'livekit'" on EVERY scheduled outbound dispatch — reminders/cascade-rebook never fired in prod | `backend/requirements.txt` (what Render installs via `pip install -r backend/requirements.txt`) never listed the LiveKit SDK. The agent host (Fly) has it, the backend host (Render) did not. So the reminder/cascade/follow-up jobs' `from livekit import api` raised ImportError → create_dispatch threw → no call. The #149/#151 fixes (window + flip-after-dispatch) were necessary but the dispatch could never succeed without the package. Proven by GET /health/voice-plane on Render: voice_plane_configured=true, all creds present, but livekit_reachable=false / "No module named 'livekit'" | 06-22 fix | ⚠ live probe. Added `livekit-api==1.1.0` (the dispatch client `LiveKitAPI`+`CreateAgentDispatchRequest`; no rtc/audio needed) to backend/requirements.txt. PROOF: re-probe /health/voice-plane after the Render redeploy must show livekit_reachable=true; then the next qualifying appointment's reminder fires end-to-end |
| 153 | 06-22 | Task 1: treatment_notes model + migration (treatment progress feature, Task 1) | NEW ORM model for treatment notes: one row per patient visit to track steps_performed, next_steps, next_reporting_date, is_final. Links to patient/doctor/branch (RULE 1 branch_id + indexes for multi-tenant queries) and optionally to token_id. Indexes on (branch_id, patient_id, visit_date) for "history" queries and (branch_id, doctor_id) for "clinic queue" queries. | 06-22 feat | ✅ test_treatment_note_model. Created backend/models/schema.py::TreatmentNote (Mapped/mapped_column per spec; ondelete=RESTRICT for PII tables, SET NULL for created_by_user_id). Migration r15treatmentnotes2026 (new table + 6 indexes, down=q14callscore2026, no downgrade risk). TDD: wrote test → ImportError (RED), implemented model → GREEN. Full unit suite 241 pass (known test_smallest_voice 500 pre-existing). |
| 154 | 06-22 | Task 2: completion resolver (treatment progress feature, Task 2) — decide whether a treatment is finished, used by the treatment-notes API in a later task | PURE logic helper, no DB/IO: a treatment closes when the 'Mark complete' button sets the flag truthy OR the doctor typed exactly 'end' (case-insensitive, whitespace-trimmed) as next_steps. Partial words ('ending soon', 'send report') must NOT close it; None/empty → still open. | 06-22 feat | ✅ test_treatment_logic. Created backend/services/treatment_logic.py::resolve_is_final(is_final_flag: bool \| None, next_steps: str \| None) -> bool — `if is_final_flag: return True; return (next_steps or "").strip().lower() == "end"`. TDD: wrote 4 tests (button, 'end' case-insensitive/trimmed, partial-word-not-closed, default-open) → ModuleNotFoundError (RED), implemented → GREEN. Full unit suite 245 pass (known test_smallest_voice 500 pre-existing). |
| 155 | 06-22 | Task 3: treatment-notes API (treatment progress feature, Task 3, M1) — create/edit a visit note, per-patient timeline, clinic/doctor patient dropdown. NO follow-up enqueue yet (M2/Task 6). | NEW router exposing 4 routes under `/treatment`: POST/PATCH note, GET timeline, GET branch patient dropdown. RULE 1 enforced in THREE layers — `forbid_admin` router dep (super_admin 403), `assert_branch_access` per handler, and every query filtered by `branch_id`. RULE 9: steps_performed/next_steps stored only, never logged with PII, never returned cross-branch. Pydantic validates visit_date not-future and next_reporting_date ≥ visit_date. | 06-22 feat | ✅ test_treatment_notes_api (3 tests: create+list timeline `active`, `next_steps="end"` → is_final, cross-branch POST → 403). Created backend/routers/treatment.py (NoteIn/NoteOut, 4 routes, `# M2 (Task 6) hooks enqueue here.` markers); registered in backend/main.py `/treatment`. TDD: wrote tests → 404 route-missing (RED), implemented + registered → GREEN. Seed adjustments vs brief skeleton (create_all enforces FKs): seed Organization (Branch.org_id FK), use whatsapp_number (NOT NULL/unique; brief used nullable did_number), Doctor.booking_type (NOT NULL), User row for created_by_user_id FK, CurrentUser jti arg. Full suite 392 pass / 2 fail — both PRE-EXISTING and unrelated: test_smallest_voice clone (live 500) + test_bug_fixes::test_confirm_booking_transient_calendar_failure_single_row (FIXLOG #23, fails on clean tree without my change — tenacity/Py3.14 drift, out of scope). |
| 156 | 06-22 | Task 5: FollowupTask thread link (treatment progress feature, Task 5, M2) — follow-up calls must form a thread linked to a treatment note and record who scheduled them. | Additive schema extension of the existing `FollowupTask` (reused for reminders/cascade-rebook): two nullable FK columns. `treatment_note_id` → treatment_notes (RESTRICT, indexed `ix_followup_tasks_treatment_note_id`) so a note can't be deleted while a follow-up references it; `created_by_user_id` → users (SET NULL) so the thread survives user deletion. `task_type` (existing app-side VARCHAR) additionally carries `"next_visit_book"`/`"doctor_advice"` — no DB enum change. | 06-22 feat | ✅ test_followup_thread_model (links FollowupTask→TreatmentNote, asserts treatment_note_id + task_type round-trip). Modified backend/models/schema.py (2 mapped_column on FollowupTask, after token_id). Migration alembic/versions/s16followupthread2026_followup_thread.py (revision `s16followupthread2026`, down_revision `r15treatmentnotes2026` confirmed via `alembic heads`; add_column ×2 + create_index; downgrade drops all 3). TDD: wrote test → `TypeError: 'treatment_note_id' is an invalid keyword argument` (RED) → implemented → GREEN. Brief's verbatim test seed omitted required cols (create_all enforces FKs/NOT NULL): added Organization (Branch.org_id FK), whatsapp_number (NOT NULL/unique), Doctor.booking_type (NOT NULL) — same adjustments as Tasks 3/4. Full unit suite 246 pass / 1 fail — pre-existing unrelated test_smallest_voice clone (live 500); the other known failure (test_bug_fixes #23) lives in tests/integration, not this suite. |
| 157 | 06-22 | Task 4: PWA Treatments view (treatment progress feature, Task 4, M1) — receptionist/doctor/owner UI for the Task-3 `/treatment` API: patient dropdown → visit-note timeline → add-note form with a "Mark treatment complete" toggle. | NEW page consuming the 3 `/treatment` endpoints. Reused existing patterns verbatim: `useAuth().branchId` for the tenant (RULE 1, same as Queue/WalkIn), `fetchDoctors(branchId)` for the note's doctor list, TanStack Query for fetch + mutate, `toast` (sonner), `revealStagger`, and the shared `.card/.field/.chip-*/.btn-primary` classes. Note's doctor defaults to the selected patient's treating doctor. On POST success the `["treatment-notes", patientId, branchId]` query is invalidated so the timeline refreshes; the complete toggle sends `is_final: true` (and clears next_reporting_date). Touch targets ≥56px on the selects/date inputs/toggle. PII: only `…{phone_last4}` shown, never full phone. | 06-22 feat | ⚠ manual UI (frontend has no JS test harness). Created frontend/src/api/treatment.js (listTreatmentPatients/listNotes/createNote — verbatim from brief, imports `{ api }`) + frontend/src/pages/Treatments.jsx. Wired guarded `/treatments` route in App.jsx (`Protected roles={["org_admin","doctor","receptionist"]}`, mirrors the `/queue` block — NOTE the existing guard component is named `Protected`, not `ProtectedRoute`) + "Treatments" nav link in components/Shell.jsx for receptionist/org_admin/doctor. PROOF: `npm run build` clean (157 modules, 2.62s, no import/JSX errors; the gsap dynamic-import warning is pre-existing/unrelated). MANUAL path: log in as receptionist → /treatments → pick a patient → add a note with next_steps="end" → timeline shows the new note + status flips to "Completed". No new deps; no offline queue (out of scope). |
| 158 | 06-22 | Task 6: idempotent next-visit follow-up enqueue (treatment progress feature, Task 6, M2) — a saved treatment note must spawn AT MOST ONE pending `next_visit_book` follow-up per (patient, doctor); a newer note supersedes the prior pending task; a final note cancels and enqueues nothing. | NEW service `backend/services/treatment_followup.py::sync_note_followup` — first `_cancel_pending` flips any pending `next_visit_book` for (patient_id, doctor_id) to `completed` (superseded, NOT dialed) via a single `UPDATE`; then, if `is_final` → stop; if neither `next_reporting_date` nor `followup_question` → stop; else add ONE pending task. RULE 9: task carries ONLY operational fields (branch/doctor/patient/treatment_note_id/what_to_ask=followup_question/scheduled_date=visit_date+1day, channel='voice') — never steps_performed/next_steps. Wired into BOTH `create_note` and `edit_note` at the M2 hook (after `db.refresh(note)`, so the note is already persisted — service's own `commit()` does not double-commit a fresh note). `NoteIn` gains `followup_question: str|None (max_length=2000)`. | 06-22 feat | ✅ test_treatment_enqueue (3 tests: enqueue creates one next_visit_book scheduled visit+1 / what_to_ask=question / pending+voice; newer note cancels prior → 1 pending survives; final note cancels + 0 pending). TDD: import-missing (RED) → implement → GREEN. Seed adjustments vs brief skeleton (create_all enforces FKs/NOT NULL, same as Tasks 3/5): seed Organization (Branch.org_id FK), whatsapp_number + unique did_number (NOT NULL/unique), Doctor.booking_type (NOT NULL). Task-3 router tests (test_treatment_notes_api) re-run GREEN — no transaction regression. Full unit+integration suite 396 pass / 2 fail — both PRE-EXISTING and unrelated: test_smallest_voice clone (live 500) + test_bug_fixes::test_confirm_booking_transient_calendar_failure_single_row (FIXLOG #23). [UPDATE 06-22 review fix: `_cancel_pending` now DELETEs superseded pending tasks instead of marking them `completed` — a pending task was never dialed, so completing it left a phantom row (ghost in the upcoming thread + over-counted "completed" follow-ups in reporting). Only `status=='pending'` rows are touched (an in_progress dispatched task is never affected → no live call dropped); FK-safe (nothing references followup_tasks.id). `update(...).values(status="completed")` → `delete(...)` over the same WHERE. Tests strengthened: newer-note asserts TOTAL next_visit_book count == 1 (old one gone, not lingering) + no `completed` row; final-note asserts TOTAL == 0 + no `completed` row. test_treatment_enqueue 3 pass, test_treatment_notes_api 3 pass, full unit+integration 396 pass / same 2 pre-existing fails.] |
| 159 | 06-22 | Task 7: follow-up THREAD endpoints (treatment progress feature, Task 7, M2) — read a patient's follow-up thread and let a doctor reply, which queues a `doctor_advice` call task that fires ASAP. | ADDED 2 routes to the existing `/treatment` router (Task 3 file, NOT a new router): `GET /patients/{patient_id}/followups?branch_id=` returns the thread (only `task_type in ("next_visit_book","doctor_advice")` for that patient AND branch, ordered `created_at` asc — superseded `next_visit_book` rows were already DELETEd by Task 6's enqueue so no phantoms) and `POST /patients/{patient_id}/followups` creates `FollowupTask(task_type="doctor_advice", channel="voice", what_to_ask=message, scheduled_date=date.today(), status="pending", created_by_user_id=<user>)`. RULE 1 three layers: `forbid_admin` router dep + `assert_branch_access(user, str(branch_id|body.branch_id))` in BOTH handlers + every query filtered by `branch_id`; POST also `_load_patient(patient_id, branch_id)` (404 if patient not in branch). RULE 9: the doctor's message (any language) is relayed verbatim as `what_to_ask` — NEVER steps_performed/next_steps. `ReplyIn` validates message min_length 1 / max_length 2000. | 06-22 feat | ✅ test_followups_api (3 tests: POST reply → 201 + one `doctor_advice` pending voice task scheduled today with created_by_user_id; GET thread returns ONLY next_visit_book+doctor_advice ordered asc, `post_appt_check` hidden; cross-branch POST → 403). TDD: wrote tests → 404 routes-missing (RED) → added `ReplyIn` + `list_followups` + `doctor_reply` to backend/routers/treatment.py → GREEN. Seed adjustments vs brief skeleton (create_all enforces FKs/NOT NULL, same as Tasks 3/5/6): Organization (Branch.org_id FK), whatsapp_number (NOT NULL/unique), Doctor.booking_type (NOT NULL), a real User row for created_by_user_id FK, CurrentUser jti arg. Task-3 router tests (test_treatment_notes_api) re-run GREEN — no regression. Full unit+integration suite 406 pass / 3 fail — 2 are the KNOWN pre-existing unrelated fails (test_smallest_voice clone live 500 + test_bug_fixes #23) and 1 is an ADDITIONAL pre-existing fail OUTSIDE this task's scope: test_bounty_round1::test_reminder_window_midnight_wrap (pure `backend/jobs/pre_appt_reminder.py` window logic — fails in isolation on a clean tree, I never touched jobs/; flagged as a concern for a separate fix, not in this task's known-failures list). |
| 160 | 06-22 | Task 8: `next_visit_followup_caller` job (treatment progress feature, Task 8, M2) — an APScheduler job that dispatches the outbound treatment follow-up calls (`next_visit_book` + `doctor_advice`) during calling hours, fed by the FollowupTask rows Tasks 6/7 enqueue. | NEW job `backend/jobs/next_visit_followup_caller.py` with a PURE `_is_due(task, now_ist)` helper (no DB/network — the unit-test target) and `run_next_visit_followups(now=None)`. Calling hours 09:00–20:00 IST (DPDP courtesy, RULE 8): `_is_due` returns False outside [9,20); `next_visit_book` fires at/after 09:00 on its scheduled day (scheduled_date ≤ today), `doctor_advice` fires the next tick (no scheduled_date gate beyond today). Run loop pulls only `status='pending' AND channel='voice' AND task_type IN (next_visit_book,doctor_advice)`, then per due task: re-loads the linked TreatmentNote — if `is_final` (treatment closed since enqueue) → `status='completed'`, skip (NOT dialed); no phone → `status='unreachable'`, skip; else increment attempt_count, flip `in_progress`, dispatch. Dispatch reuses the reminder pattern (`lk_api.LiveKitAPI()`, `CreateAgentDispatchRequest`, `branch_outbound_trunk_id`, `agent_dispatch.create_dispatch`). RULE 9: metadata carries ONLY operational fields — call_type/branch_id/outbound_trunk_id/phone_number/task_id/patient_name/doctor_name/doctor_id/message(=what_to_ask) + (booking) target_date(=note.next_reporting_date)/window=2 — NEVER steps_performed/next_steps. Self-healing retry mirrors the reminder job: on dispatch exception `status='unreachable'` if attempt_count ≥ max_attempts else back to `pending` for the next tick (never marked done before a successful dispatch). RULE 1: every load filtered by id; the FollowupTask rows are already branch-scoped at enqueue. Registered in `backend/main.py` lifespan leader (`if got:`) block with `IntervalTrigger(minutes=15)`, id `next_visit_followups`. | 06-22 feat | ✅ test_next_visit_followup_caller (5 tests, PURE `_is_due`, no DB/network: book due after 9am / not due before 9am / doctor_advice due within hours / nothing due at night (22:00) / future scheduled_date not due). TDD: import-missing (RED) → implement → 5 GREEN. Implementation is verbatim from the Task-8 brief (job + helper + main.py registration). main.py + job import clean. Full unit+integration suite 412 pass / 2 fail — both PRE-EXISTING and unrelated to this task: test_smallest_voice clone (live 500) + test_bug_fixes::test_confirm_booking_transient_calendar_failure_single_row (FIXLOG #23). (The Task-7 run also flagged test_bounty_round1::test_reminder_window_midnight_wrap as a pre-existing isolation-only fail; it did NOT fail in this full-suite run.) [UPDATE 06-22 review fix: DISPATCH-THEN-MUTATE + COMMIT-PER-TASK. The original loop set `status='in_progress'` BEFORE `_dispatch` with one end-of-loop commit; the run query only pulls `pending` and NO job requeues a stranded `in_progress` FollowupTask (`requeue_stale_in_progress` handles CalendarWriteTask only), so a crash/throw after the flip stranded the task forever — exactly FIXLOG #151's permanent-miss. Now mirrors `pre_appt_reminder` `run_pre_appt_reminders`/`_dispatch_reminder_call`: `_dispatch` returns bool (True on create_dispatch success, False on exception — logged, never raised); the loop increments attempt_count, dispatches FIRST, then on True → `completed` (call dispatched; agent enriches response_summary on call-end; non-pending so the next 15-min tick won't re-dial) / on False → `unreachable` if attempt_count ≥ max_attempts else stays `pending` (self-healing retry). `await db.commit()` is now PER-TASK (after each state mutation incl. the missing-data and final-note skips), so one task's exception can't roll back the batch. Issue 3 (RULE 9): target_date/window now gated on `task_type=='next_visit_book'` so a doctor_advice linked to a note never gets a spurious booking target_date. Added DB-backed run-loop test test_next_visit_followup_caller_run.py (3 tests, `_dispatch` monkeypatched → no network: success→completed + target_date present + no steps_performed/next_steps; failure→pending + attempt_count==1; final note→completed + never dialed). PROOF: 5 unit + 3 integration GREEN; full unit+integration 415 pass / 2 pre-existing fails (test_smallest_voice live 500 + test_bug_fixes #23).] |
| 162 | 06-23 | Task 10: data-retention/erasure job (DPDP RULE 9) didn't touch treatment health-data — when a stale patient's PII was anonymised, their TreatmentNote rows and follow-up thread health text survived | The job (backend/jobs/data_retention.py::run_data_retention) cleared name/phone/age/gender + stamped anonymized_at but left treatment_notes.steps_performed/next_steps and FollowupTask.what_to_ask (doctor's question/advice) / response_summary (patient's reply) — health-adjacent PII that must die with the patient. | 06-23 feat | ✅ test_data_retention_treatment::test_anonymise_wipes_treatment_notes_and_thread_text (RED: TreatmentNote survived → GREEN: deleted + thread text NULL). Inside the existing `for p in stale:` loop, after anonymised_at: NULL the patient's FollowupTask (treatment_note_id=None, what_to_ask=None, response_summary=None) FIRST, THEN delete their treatment_notes. FK ORDER is load-bearing — FollowupTask.treatment_note_id -> treatment_notes is ondelete=RESTRICT, so clearing the link must precede the delete or it raises FK violation; the task rows are kept (non-PII outcome trends). All scoped by patient_id == p.id, same `db` session + existing end-of-loop commit (gated on `stale` non-empty). Proof: focused test green; the 5 existing test_data_retention integration tests still green; full unit+integration suite 418 passed (only the 2 known pre-existing failures: test_smallest_voice live-500 + test_confirm_booking_transient_calendar_failure #23). |
| 163 | 06-23 | Task 11 (FINAL): PWA follow-up THREAD panel + "Reply to patient" box + needs-attention badge (treatment progress feature, Task 11, M2) — surface the Task-7 thread endpoints in the existing Treatments view so a doctor sees the captured patient reply and can relay advice. | NEW UI on frontend/src/pages/Treatments.jsx, under the visit timeline: a "Follow-up thread" card rendering each `{task_type, message, response, status}` as a chat-style row (clinic/agent message left in teal-mint, patient reply right in amber, visually distinct) + a "Reply to patient" textarea/send button that POSTs replyToPatient with `branch_id`, `doctor_id` (the selected patient's treating doctor — SAME source the add-note form uses) and `message`. A "needs attention" badge shows when ANY thread item has a non-empty `response` or `status === "unreachable"`. Reused existing patterns VERBATIM: `useAuth().branchId` (RULE 1 tenant), TanStack Query fetch (`["followups", patientId, branchId]`) + `useMutation`, `toast` (sonner), shared `.card/.field/.chip-*/.btn-primary/.label` classes, the timeline section/header layout. On send, invalidates `["followups", patientId, branchId]` so the thread refreshes. API: added `listFollowups(patientId, branchId)` (GET → `.data.thread`) + `replyToPatient(patientId, payload)` (POST → `.data`) to frontend/src/api/treatment.js, matching the existing `{ api }` import + function style. PII: only the operational message/reply text is rendered (the thread endpoint is already RULE-1 branch-scoped server-side); no full phone shown. | 06-23 feat | ⚠ manual UI (frontend has no JS test harness). PROOF: `cd frontend && npm run build` clean (157 modules, 8.08s, no import/JSX errors; the gsap dynamic-import warning is pre-existing/unrelated). MANUAL path: log in → /treatments → pick a patient with follow-up history → thread shows the doctor's question + the captured patient reply (amber, right) + a "needs attention" badge → type advice in "Reply to patient" → Send → mutation succeeds, `["followups",…]` invalidated, a new `doctor_advice` row appears `pending`. No new deps; no offline queue (out of scope). |
| 161 | 06-23 | Task 9: agent treatment follow-up flows (feature, M2) — next_visit_book (ask doctor's question + book ±2 days) and doctor_advice (relay doctor's advice verbatim) outbound call types + capture the patient's reply | NEW agent behaviour in agent/livekit_minimal/agent.py. `NEXT_VISIT_PROMPT_EXTRA` / `DOCTOR_ADVICE_PROMPT_EXTRA` encode RULE 7 (agent RELAYS the doctor's {message}, gives NO advice/diagnosis/triage; on any reported problem says "I'll inform the doctor and get back to you"). `_FOLLOWUP_CALLTYPES` + `is_followup`; `_followup_meta_safe(meta)` allow-lists ONLY operational keys (call_type/message/target_date/window/patient_name/doctor_name/doctor_id/task_id) so steps_performed/next_steps can NEVER reach the LLM/TTS (RULE 9 + RULE 6 — doctor text enters via the prompt, never raw to TTS). Booking reuses the existing atomic token tools (RULE 2). | 06-23 feat | ✅ test_agent_followup_metadata (2 tests: both prompt extras relay-only + contain {message} + "inform the doctor"; _followup_meta_safe excludes steps_performed/next_steps). Wiring: `is_followup`/`followup_meta` derived once; two `instructions +=` branches use followup_meta (the safe dict, not raw meta); an `elif is_followup` OPENING-GREETING branch greets the known patient by name (NOT the inbound disclosure path → no spurious inbound Consent on an outbound call); `_cleanup_on_shutdown` write-back persists the patient's spoken reply (user turns from session.history) → FollowupTask.response_summary + status=completed in its OWN short-lived branch-scoped session (best-effort, never breaks teardown; idempotent with Task 8's dispatch-time completed). agent.py parses; full unit suite green except known test_smallest_voice live-500. (Completed inline after the implementer subagent hit the session limit mid-wiring; sent to review.) |
| 164 | 06-23 | RULE 1 write-hygiene — the treatment router accepted `doctor_id` on writes without confirming the doctor belongs to the asserted branch. A client with legitimate access to branch A could attach branch B's `doctor_id` to a note/follow-up task (cross-tenant data-integrity issue, RULE 1 write-side). | Added `_load_doctor(doctor_id, branch_id, db)` to backend/routers/treatment.py (mirrors the existing `_load_patient` shape: `select(Doctor).where(Doctor.id == doctor_id, Doctor.branch_id == branch_id)` → 404 "doctor not found in branch" if None). Called it in ALL THREE write handlers right after `assert_branch_access` (and after `_load_patient` where present): `create_note`, `edit_note`, `doctor_reply`. Imported `Doctor` from backend.models.schema. Read GETs untouched (no doctor_id on the body). No other behaviour changed. | 06-23 fix | PROOF (TDD): added one focused 404 test per affected route — `test_treatment_notes_api.py::test_cross_branch_doctor_id_rejected_on_write` (covers create_note + edit_note with another branch's doctor_id → 404) and `test_followups_api.py::test_cross_branch_doctor_id_rejected_on_reply` (doctor_reply → 404); both fail without the `_load_doctor` calls, pass with them. Targeted suite (test_treatment_notes_api + test_followups_api + test_treatment_enqueue) 11/11 green; full `pytest tests/unit tests/integration` 420 passed, only the two KNOWN unrelated failures remain (test_smallest_voice live-500, test_confirm_booking_transient_calendar_failure_single_row FIXLOG #23). Happy-path seeds already created a real in-branch Doctor and passed its id, so no seed weakening was needed. |
| 165 | 06-23 | Sign-in dead on the live site — every auth route (`/auth/login`, `/auth/register`, `/auth/request-otp`) returned HTTP 500 even though the backend and DB were healthy (`/health` 200). | Prod Redis was unreachable from Render (Render's `REDIS_URL` env was stale/dropped during the recent free-plan reconfig). The rate limiter, IP blocklist and failed-login counter all raised on the Redis error and FastAPI surfaced it as 500 — a Redis blip became a total auth outage. The Redis instance itself was alive (local PING via the same Upstash URL succeeded; the exact rate-limit path ran clean locally), proving the fault is Render env, not code or instance. | 06-23 fix | DURABLE CODE FIX (RULE 8): `backend/middleware/rate_limit.py` now FAILS OPEN on any Redis error in the throttling layer — `_rate_limit_dep` (allow + log `rate_limit_redis_unavailable`), `is_ip_blocked` (return False + log), `record_failed_login` (swallow + log). Degraded throttling beats a dead login; token-locking for bookings stays fail-CLOSED elsewhere (never double-book). PROOF: `tests/security/test_rate_limit_failopen.py` (4 tests, Redis getter patched to raise → dep returns None/allowed, is_ip_blocked False, check_ip_blocklist passes, record_failed_login no-raise) — 4 passed. OPS (Vinay): restore the correct `REDIS_URL` on Render to re-enable rate limiting + atomic token locking (booking path needs Redis). |
| 166 | 06-23 | Trial clinics showed their plan's minute bucket (solo = 100) instead of the 500-minute free-trial grant on the dashboard donut + super-admin view. | `backend/routers/analytics.py` computed `included = PLAN_MINUTES[plan]` and `admin.py` used `included_minutes(o.plan)` — neither consulted org status, so a trial org got the plan allowance the comment even claimed was 500. | 06-23 fix | Added `TRIAL_MINUTES = 500` + `included_minutes_for(plan, status)` to `backend/services/billing_math.py` (status=="trial" → 500, else plan bucket) as the single source. analytics overview now selects `Organization.status` and calls `included_minutes_for`; admin clients view calls `included_minutes_for(o.plan, o.status)`; dead `PLAN_MINUTES` constant removed. PROOF: `tests/unit/test_billing_math.py::test_trial_org_gets_flat_500_minutes_regardless_of_plan` + `test_non_trial_org_gets_plan_bucket` — 10 passed; analytics+admin import clean. |
| 174 | 06-24 | Live call broke — agent spoke "garbage, not Telugu" and kept talking. | The agent's primary LLM (Gemini 2.5 Flash) was 503-ing intermittently (Google-side overload spike, ~20-40% that day). The `FallbackAdapter` switched to GPT-4o-mini on the FIRST 503 — and GPT-4o-mini's Telugu is much weaker → "not Telugu, garbage"; the LLM-error churn read as "keeps talking". | 06-24 fix | `_build_fallback_llm` now: (a) inserts a SECOND Telugu-strong Gemini tier (`gemini-2.5-flash-lite`) before GPT-4o-mini, and (b) sets `max_retry_per_llm=1` + `retry_interval=0.4` so a transient 503 retries on the SAME model first (a 503 returns fast → cheap retry). GPT-4o-mini is now reached only if BOTH Gemini tiers fail twice — very unlikely even under overload. Verified `_build_fallback_llm()` constructs; needs `fly deploy`. |
| 176 | 06-24 | Live calls broken by Gemini 2.5 Flash 503 outage (30-80% fail, 5s+ latency, Google-side spike) — agent garbled + slow regardless of our code. | gemini-2.5-flash (and 2.5-flash-lite) were on an overloaded Google capacity pool that day. | 06-24 fix | Switched both the agent's primary LLM (`_build_fallback_llm`) and the routing LLM to **gemini-3.1-flash-lite** (thinking_budget=0). Live probe across all available Gemini models: 3.1-flash-lite = 6/6 available, ~1.5s, natural Telugu — vs 2.5-flash 4/6 + 503s, and the bigger flash models (3.5-flash 50s, flash-latest 15s, 3-flash-preview 5.6s) too slow for a phone (thinking ON). GPT-4o-mini stays the fast fallback. Also shipped: LLM retry revert (no 9.5s storm) + EOU 1.0→0.6. Constructs clean; Telugu verified. Needs fly deploy. |
| 175 | 06-24 | Clinic name mispronounced — "Datta"/"దత్త" spoken by TTS as English "data". | The clinic name (`branch.name`, stored Latin) was passed verbatim into the welcome clip + system prompt + greetings; smallest.ai TTS reads a Latin word in a Telugu sentence as English (RULE 6 — same class as the doctor-name spelling bug). | 06-24 fix | NEW `Branch.name_spoken` (migration `v19clinicspoken2026`, additive nullable) — the clinic name transliterated into the call's script. The agent uses `name_spoken` if set; otherwise transliterates `branch.name` once via `spoken_name` (Sarvam, proven: "Sri Datta"→"శ్రీ దత్త") and stores it asynchronously (off the call path, never blocks the instant welcome clip). `scripts/backfill_clinic_spoken.py` pre-populates existing clinics. Needs migration + `fly deploy` + backfill run. |
| 173 | 06-24 | Rate limiter (+ IP blocklist + failed-login throttle) silently DISABLED in prod — 7 rapid /auth/request-otp calls all 200, never 429. A real security hole: auth/OTP unthrottled. | `client_ip()` keys on `XFF[-trusted_proxy_hops]`; with hops=2 behind Cloudflare→Render the XFF chain is `[real-client, cloudflare-edge, render-proxy]`, so it resolved the MIDDLE entry = a Cloudflare EDGE IP that ROTATES every request (proven via `/health/ratelimit`: resolved_client_ip = 162.158.54.36 → 172.68.147.204 → 172.71.124.143 across 3 probes, while cf-connecting-ip stayed 223.187.27.252). A new key per request → the limiter counter never accumulates → no 429. Redis itself was healthy (`/health/redis` ok). | 06-24 fix | `client_ip()` now prefers Cloudflare's authoritative `CF-Connecting-IP` (then `True-Client-IP`) when present — set by Cloudflare, not client-spoofable through it, and STABLE per client — before the XFF/hops fallback. PROOF: `tests/security/test_rate_limit_failopen.py::test_client_ip_prefers_cf_connecting_ip_and_is_stable` (rotating edge XFF + stable CF header → same key both requests) + `test_client_ip_falls_back_to_hops_without_cf_header` (XFF/hops path preserved when no CF header); 6 passed. Added diagnostics `/health/redis` + `/health/ratelimit`. Verified in prod after deploy: 7 rapid request-otp → 429 fires. |
| 172 | 06-24 | Three long-standing "known-failing" tests (test_smallest_voice clone, test_confirm_booking transient-calendar, test_rate_limit user-quota) — none were real product bugs, but they masked regressions and made the suite untrustworthy. | (a) clone test: `clone_voice()` posts with raw httpx (not the mocked SDK), so the test hit the LIVE smallest.ai API → 500. (b) booking test: expected 3 calendar calls but the retry was deliberately reduced to `stop_after_attempt(2)`; the test was never updated. (c) rate-limit test: ran the limiter against prod Upstash (.env REDIS_URL) — 60+ requests from a dev box exceed the 60s window before the cap is hit, so it never 429s (limiter is fine; it's latency). | 06-24 test | (a) mock `httpx.post` in the clone test. (b) align to the 2-attempt policy: `FlakyCalendar(failures=1)`, assert `cal.calls==2` (single-row guarantee preserved). (c) `tests/conftest.py` repoints the suite's Redis to the LOCAL Docker redis (env `TEST_REDIS_URL`, default `redis://localhost:6379/0`) — hermetic, fast, never burns Upstash quota; matches the test file's own "real Redis (Docker)" intent. Also allowlisted the documented Fly build-dummy JWT_SECRET (commit 8191674). PROOF: all three now pass; full `pytest tests/unit tests/integration tests/security` = **543 passed, 3 skipped, 0 failed** (was 4 failed). |
| 171 | 06-24 | No way to bill — or even see — voice-minute OVERAGE; the BillingCycle table had overage columns but nothing computed or charged it, so per-minute overage Razorpay billing was unverified. | Missing overage-billing path. | 06-24 feat | `billing_math.overage_breakdown(plan, minutes_used, status, adjustment)` — single source for the itemised overage bill + the exact paise sent to Razorpay (GST_RATE=0.18 on top; CLAUDE.md prices are ex-GST). NEW `backend/services/overage_billing.py::create_overage_order` (computes the breakdown, creates a Razorpay order with server-set notes; returns None for ₹0 — Razorpay rejects zero-amount). NEW `scripts/generate_fake_bill.py` — prints the itemised bill and creates a real Razorpay order (`python -m scripts.generate_fake_bill [plan] [minutes] [--dry]`). PROOF: `test_billing_math.py` (3 overage cases incl. the solo/1000-min scenario → 900 × ₹5 + 18% GST = ₹5,310 = 531000 paise; no-overage=0; respects minute adjustment) + `tests/unit/test_overage_billing.py` (2: correct amount+notes via mocked client; no order within bucket) — 16 + 2 passed. LIVE PROOF: ran the script against TEST Razorpay keys → real sandbox order `order_T5B50JhZwohb0Z`, 531000 paise, status=created (visible in the Razorpay test dashboard → Orders). |
| 170 | 06-23 | Clinics couldn't change their own plan — only the super-admin could, and it applied immediately (a mid-month downgrade could shrink minutes already paid for). | Feature gap: no self-serve, cycle-aware plan switch. | 06-23 feat | NEW `organizations.pending_plan` + `pending_plan_effective` (migration `u18pendingplan2026`, additive nullable). `billing_math.next_cycle_start(today)` → 1st of next month (year-wrap safe). Clinic-facing `GET /api/plan` + `POST /api/plan-change` {plan} (org_admin only; schedules the switch for next cycle, never mid-month; selecting the current plan cancels a pending change). Daily job `run_pending_plan_changes` (trial_pause.py, registered in main.py leader, 6h) promotes pending→plan once effective ≤ today. Settings.jsx "Plan & billing" section (selector + scheduled-change InfoBox). API fns fetchPlan/changePlan. PROOF: `tests/integration/test_plan_change.py` (7 tests: next_cycle_start math incl. Dec→Jan wrap; schedule sets pending+effective; current-plan cancels; non-admin 403; invalid plan 422; apply job promotes due / skips future) — 7 passed; `npm run build` clean. |
| 169 | 06-23 | Super-admin console lacked: per-clinic minute adjustment, a hard delete, and a 'cancelled' (block) state — only pause/plan/hard-block existed. | Feature gap in the admin org controls. | 06-23 feat | (a) NEW `organizations.minutes_adjustment` (migration `t17minutesadjust2026`, additive int default 0) + `billing_math.included_minutes_for(plan, status, adjustment)` floored at 0; analytics donut + admin clients view now add the adjustment. (b) `POST /admin/orgs/{id}/minutes` {adjustment}. (c) `/orgs/{id}/status` now accepts `cancelled` (block) alongside active/paused. (d) `DELETE /admin/orgs/{id}` — `_hard_delete_org` removes all tenant data in FK-safe order (followup_tasks→treatment_notes→calendar/call*/consent→tokens→doctor_unavailability→patients→doctors→whatsapp→branches→users→billing→org; every tenant FK is RESTRICT so order is load-bearing), guarded so an `active` org returns 409 (must pause/cancel first), audited. Admin.jsx: "Adjust minutes" (prompt) + "Delete clinic" (confirm) buttons; overview row carries `minutes_adjustment`. PROOF: `test_billing_math.py::test_minutes_adjustment_applies_and_floors_at_zero` + `tests/integration/test_admin_delete_org.py::test_hard_delete_removes_all_tenant_data` (seeds full token→note→followup+billing+user chain, deletes, asserts every table empty — fails on any wrong FK order). 12 targeted + 44 admin/analytics tests green; `npm run build` clean. |
| 168 | 06-23 | No password-reset path — a clinic that forgot its password was permanently locked out (only Google or a remembered password worked). | Feature gap: email/password accounts had no recovery flow. | 06-23 feat | NEW `POST /auth/forgot-password` (emails a code via the existing email-OTP service; no account-existence oracle — same 200 shape whether or not the email maps to a user) + `POST /auth/reset-password` (verifies the code, sets a new bcrypt password, signs the user in, one-shot clears the code). Frontend: "Forgot password?" link on Login.jsx → request-code → enter-code+new-password panel → auto-login. API fns `forgotPassword`/`resetPassword`. PROOF: `tests/integration/test_password_reset.py` (4 tests: full reset→login-with-new / old-password-now-401, unknown email no-oracle, weak password 422, wrong code 401) — 4 passed; `npm run build` clean. Reuses Redis-backed otp_service (needs the same REDIS_URL fix as #165 to work in prod). |
| 167 | 06-23 | Reminder call spelled the doctor's name letter-by-letter ("Srinivas" → "S R I N I") instead of saying it. | The outbound greetings (`reminder`/`rebook`/`followup`) inserted the raw `doctor_name`/`patient_name` (Latin, as the clinic typed them) into a Telugu-script sentence; smallest.ai TTS reads Latin glyphs inside an Indic sentence as individual letters (RULE 6 — names must reach TTS in the call's script). | 06-23 fix | NEW `agent/i18n/transliterate.py::spoken_name(name, lang_code)` — transliterates Latin names to the call script via the Sarvam Transliterate API (`spoken_form=True`, key already configured), cached in-process; no-ops for empty/already-Indic names and for English calls; falls back to the raw name on ANY error (RULE 8). `agent/livekit_minimal/agent.py` precomputes `_spk_patient`/`_spk_doctor` once and uses them in all three outbound greetings. PROOF: `tests/unit/test_transliterate.py` (5 tests: empty no-op, Indic no-op + no network, Latin→transliterated, cache hit = one call, failure→original) — 5 passed; agent.py parses. Sarvam endpoint verified live (`Srinivas` → `శ్రీనివాస్`). |
| 170 | 06-24 | Start-of-call gap: ~5-6s of cold dead air after pickup before the agent spoke ("even starting takes 7-10s"). | LiveKit `session.start()` takes ~6s — dominated by Sarvam STT's cold websocket connect (the plugin opens a fresh `aiohttp.ClientSession` per call at `stream()`; can't be warmed by config). The live disclosure greeting could only play AFTER that. | 06-24 perf (`ab2d0f5`) | OPTION 1 — pre-rendered welcome mask. NEW `branches.welcome_audio` BYTEA (migration `w20welcomeaudio2026`, additive nullable) holds the clinic's full welcome+greeting WAV (welcome clip + AI disclosure + "how can I help"), generated once via smallest.ai REST `/tts` (`backend/services/welcome_synth.py` — raw HTTP; the livekit smallest plugin fails outside a job context). `agent/livekit_minimal/welcome_audio.py::play_stored_welcome` parses the WAV (strip header → 10ms int16 frames → `rtc.AudioSource`) and plays it INSTANTLY on answer, masking `session.start()`; when present it IS the full opening so the live greeting is skipped (`_used_stored_welcome`). NULL → live-synth fallback (RULE 8). `scripts/backfill_welcome_audio.py` pre-bakes existing clinics in the clinic's own voice. PROOF: Vinay live call — "now latency is just 3 secs, it's good" (was 7-10s). Backfill: Sri Venkateshwara → 299KB / ~6.2s in voice_euTAvxhJLE. |
| 171 | 06-24 | Agent did not stop on barge-in — "it is finishing what it is saying, not stopping when I am speaking." | `resume_false_interruption=True` + Sarvam's slow Telugu transcript: the agent stopped on VAD, but the transcript arrived after the 1.5s false-interruption window, so a real interruption was judged "false" and the sentence RESUMED. | 06-24 fix | Disabled `resume_false_interruption` (a detected interruption now stays stopped); `min_interruption_duration` 0.3→0.2, `min_interruption_words=1` (yield on first word). Trade-off noted in code: a lone backchannel can now cut the agent — revisit with a Telugu backchannel filter if it surfaces. PROOF: ⚠ live call — Vinay confirmed "now its good" (barge-in stops + stays stopped). |
| 172 | 06-24 | Pre-rendered welcome (#170) played choppy — "intro is breaking, words are breaking." | The `rtc.AudioSource` default queue is 1000ms; `session.start()` connects on the same event loop concurrently and starves the welcome's frame-pump, so `capture_frame` blocked on backpressure mid-clip and playout underran. | 06-24 fix | `play_stored_welcome` now sizes `queue_size_ms` to the full clip duration + 1s, so all frames buffer into the rtc engine in one pass and playout is engine-driven, immune to Python-loop blocking. PROOF: ⚠ live call — Vinay confirmed "now good" (smooth intro). |
